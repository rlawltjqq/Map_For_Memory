
const PROV_KR = {"39": "제주", "38": "경남", "37": "경북", "36": "전남", "35": "전북", "34": "충남", "33": "충북", "32": "강원", "31": "경기", "29": "세종", "26": "울산", "25": "대전", "24": "광주", "23": "인천", "22": "대구", "21": "부산", "11": "서울"};
const PROV_JP = {"91": "홋카이도", "92": "도호쿠", "93": "간토", "94": "주부", "95": "긴키", "96": "주고쿠", "97": "시코쿠", "98": "규슈", "99": "오키나와", "9T": "도쿄"};
const PROV = Object.assign({}, PROV_KR, PROV_JP);   // 코드 앞 2자리 → 시·도 / 지방
const EMBLEMS = {"38400": "emblems/38400.png", "38390": "emblems/38390.png", "38380": "emblems/38380.png", "38370": "emblems/38370.png", "38360": "emblems/38360.png", "38350": "emblems/38350.png", "38340": "emblems/38340.png", "38330": "emblems/38330.png", "38320": "emblems/38320.png", "38310": "emblems/38310.png", "38100": "emblems/38100.png", "38090": "emblems/38090.png", "38080": "emblems/38080.png", "38070": "emblems/38070.png", "38060": "emblems/38060.png", "38050": "emblems/38050.png", "38030": "emblems/38030.png", "37430": "emblems/37430.png", "37420": "emblems/37420.png", "37410": "emblems/37410.png", "37400": "emblems/37400.png", "37390": "emblems/37390.png", "37380": "emblems/37380.png", "37370": "emblems/37370.png", "37360": "emblems/37360.png", "37350": "emblems/37350.png", "37340": "emblems/37340.png", "37330": "emblems/37330.png", "37320": "emblems/37320.png", "37310": "emblems/37310.png", "37100": "emblems/37100.png", "37090": "emblems/37090.png", "37080": "emblems/37080.png", "37070": "emblems/37070.png", "37060": "emblems/37060.png", "37050": "emblems/37050.png", "37040": "emblems/37040.png", "37030": "emblems/37030.png", "37020": "emblems/37020.png", "36480": "emblems/36480.png", "36470": "emblems/36470.png", "36460": "emblems/36460.png", "36450": "emblems/36450.png", "36430": "emblems/36430.png", "36420": "emblems/36420.png", "36410": "emblems/36410.png", "36400": "emblems/36400.png", "36390": "emblems/36390.png", "36380": "emblems/36380.png", "36370": "emblems/36370.png", "36360": "emblems/36360.png", "36350": "emblems/36350.png", "36330": "emblems/36330.png", "36320": "emblems/36320.png", "36310": "emblems/36310.png", "36060": "emblems/36060.png", "36040": "emblems/36040.png", "36020": "emblems/36020.png", "36010": "emblems/36010.png", "35380": "emblems/35380.png", "35370": "emblems/35370.png", "35360": "emblems/35360.png", "35350": "emblems/35350.png", "35340": "emblems/35340.png", "35330": "emblems/35330.png", "35320": "emblems/35320.png", "35310": "emblems/35310.png", "35060": "emblems/35060.png", "35050": "emblems/35050.png", "35040": "emblems/35040.png", "35030": "emblems/35030.png", "35020": "emblems/35020.png", "34380": "emblems/34380.png", "34370": "emblems/34370.png", "34360": "emblems/34360.png", "34350": "emblems/34350.png", "34340": "emblems/34340.png", "34330": "emblems/34330.png", "34310": "emblems/34310.png", "34080": "emblems/34080.png", "34070": "emblems/34070.png", "34060": "emblems/34060.png", "34050": "emblems/34050.png", "34040": "emblems/34040.png", "34030": "emblems/34030.png", "34020": "emblems/34020.png", "33390": "emblems/33390.png", "33380": "emblems/33380.png", "33370": "emblems/33370.png", "33360": "emblems/33360.png", "33350": "emblems/33350.png", "33340": "emblems/33340.png", "33330": "emblems/33330.png", "33320": "emblems/33320.png", "33020": "emblems/33020.png", "32410": "emblems/32410.png", "32390": "emblems/32390.png", "32380": "emblems/32380.png", "32370": "emblems/32370.png", "32350": "emblems/32350.png", "32340": "emblems/32340.png", "32330": "emblems/32330.png", "32320": "emblems/32320.png", "32310": "emblems/32310.png", "32070": "emblems/32070.png", "32060": "emblems/32060.png", "32050": "emblems/32050.png", "32040": "emblems/32040.png", "32030": "emblems/32030.png", "32020": "emblems/32020.png", "32010": "emblems/32010.png", "31380": "emblems/31380.png", "31370": "emblems/31370.png", "31350": "emblems/31350.png", "31280": "emblems/31280.png", "31270": "emblems/31270.png", "31260": "emblems/31260.png", "31240": "emblems/31240.png", "31230": "emblems/31230.png", "31220": "emblems/31220.png", "31210": "emblems/31210.png", "31200": "emblems/31200.png", "31180": "emblems/31180.png", "31170": "emblems/31170.png", "31160": "emblems/31160.png", "31150": "emblems/31150.png", "31140": "emblems/31140.png", "31130": "emblems/31130.png", "31120": "emblems/31120.png", "31110": "emblems/31110.png", "31080": "emblems/31080.png", "31070": "emblems/31070.png", "31060": "emblems/31060.png", "31030": "emblems/31030.png", "29010": "emblems/29010.png", "26310": "emblems/26310.png", "26040": "emblems/26040.png", "26030": "emblems/26030.png", "26020": "emblems/26020.png", "26010": "emblems/26010.png", "25050": "emblems/25050.png", "25040": "emblems/25040.png", "25030": "emblems/25030.png", "25020": "emblems/25020.png", "25010": "emblems/25010.png", "24050": "emblems/24050.png", "23310": "emblems/23310.png", "23080": "emblems/23080.png", "23070": "emblems/23070.png", "23060": "emblems/23060.png", "23050": "emblems/23050.png", "23040": "emblems/23040.png", "23020": "emblems/23020.png", "23010": "emblems/23010.png", "22310": "emblems/22310.png", "22070": "emblems/22070.png", "22060": "emblems/22060.png", "22050": "emblems/22050.png", "22040": "emblems/22040.png", "22030": "emblems/22030.png", "22020": "emblems/22020.png", "22010": "emblems/22010.png", "21310": "emblems/21310.png", "21150": "emblems/21150.png", "21140": "emblems/21140.png", "21130": "emblems/21130.png", "21120": "emblems/21120.png", "21110": "emblems/21110.png", "21100": "emblems/21100.png", "21090": "emblems/21090.png", "21080": "emblems/21080.png", "21070": "emblems/21070.png", "21060": "emblems/21060.png", "21050": "emblems/21050.png", "21040": "emblems/21040.png", "21030": "emblems/21030.png", "21020": "emblems/21020.png", "21010": "emblems/21010.png", "11250": "emblems/11250.png", "11240": "emblems/11240.png", "11230": "emblems/11230.png", "11220": "emblems/11220.png", "11210": "emblems/11210.png", "11200": "emblems/11200.png", "11190": "emblems/11190.png", "11180": "emblems/11180.png", "11170": "emblems/11170.png", "11160": "emblems/11160.png", "11150": "emblems/11150.png", "11140": "emblems/11140.png", "11130": "emblems/11130.png", "11120": "emblems/11120.png", "11110": "emblems/11110.png", "11100": "emblems/11100.png", "11090": "emblems/11090.png", "11080": "emblems/11080.png", "11070": "emblems/11070.png", "11060": "emblems/11060.png", "11050": "emblems/11050.png", "11040": "emblems/11040.png", "11030": "emblems/11030.png", "11020": "emblems/11020.png", "11010": "emblems/11010.png", "31250": "emblems/31250.png", "24040": "emblems/24040.png", "24030": "emblems/24030.png", "24020": "emblems/24020.png", "24010": "emblems/24010.png", "23320": "emblems/23320.png", "23030": "emblems/23030.png", "36440": "emblems/36440.png", "36030": "emblems/36030.png", "38111": "emblems/38111.png", "37011": "emblems/37011.png", "35011": "emblems/35011.png", "34011": "emblems/34011.png", "33011": "emblems/33011.png", "31191": "emblems/31191.png", "31101": "emblems/31101.png", "31091": "emblems/31091.png", "31051": "emblems/31051.png", "31041": "emblems/31041.png", "31021": "emblems/31021.png", "31011": "emblems/31011.png", "39020": "emblems/39020.png", "39010": "emblems/39010.png", "32400": "emblems/32400.png", "32360": "emblems/32360.png", "33030": "emblems/33030.png", "33310": "emblems/33310.png", "91001": "emblems/91001.png", "92002": "emblems/92002.png", "92003": "emblems/92003.png", "92004": "emblems/92004.png", "92005": "emblems/92005.png", "92006": "emblems/92006.png", "92007": "emblems/92007.png", "93008": "emblems/93008.png", "93009": "emblems/93009.png", "93010": "emblems/93010.png", "93011": "emblems/93011.png", "93012": "emblems/93012.png", "93013": "emblems/93013.png", "93014": "emblems/93014.png", "94015": "emblems/94015.png", "94016": "emblems/94016.png", "94017": "emblems/94017.png", "94018": "emblems/94018.png", "94019": "emblems/94019.png", "94020": "emblems/94020.png", "94021": "emblems/94021.png", "94022": "emblems/94022.png", "94023": "emblems/94023.png", "95024": "emblems/95024.png", "95025": "emblems/95025.png", "95026": "emblems/95026.png", "95027": "emblems/95027.png", "95028": "emblems/95028.png", "95029": "emblems/95029.png", "95030": "emblems/95030.png", "96031": "emblems/96031.png", "96032": "emblems/96032.png", "96033": "emblems/96033.png", "96034": "emblems/96034.png", "96035": "emblems/96035.png", "97036": "emblems/97036.png", "97037": "emblems/97037.png", "97038": "emblems/97038.png", "97039": "emblems/97039.png", "98040": "emblems/98040.png", "98041": "emblems/98041.png", "98042": "emblems/98042.png", "98043": "emblems/98043.png", "98044": "emblems/98044.png", "98045": "emblems/98045.png", "98046": "emblems/98046.png", "99047": "emblems/99047.png"};   // 지역 코드 → 공식 휘장(심벌) 이미지 URL
const CLIMATE = {"38060":[[1.5,50],[3.8,47],[8.9,87],[13.5,109],[18.1,113],[22.5,197],[25.2,289],[26.5,204],[22.3,209],[15.6,54],[9.6,60],[2.0,21]],"38050":[[3.3,54],[5.1,58],[9.6,96],[13.4,116],[17.5,136],[21.5,225],[24.8,296],[26.7,213],[23.2,247],[17.4,59],[11.6,60],[4.1,23]],"38030":[[1.8,48],[4.0,44],[9.1,85],[13.7,105],[18.4,109],[23.0,186],[25.6,271],[26.8,206],[22.5,203],[15.9,48],[9.8,59],[2.3,19]],"37320":[[-0.2,29],[2.2,28],[8.1,68],[13.3,53],[18.2,101],[23.1,143],[25.3,308],[25.9,285],[21.4,162],[14.3,43],[7.9,46],[-0.0,18]],"36390":[[2.5,48],[4.3,48],[8.9,72],[13.4,106],[17.8,141],[22.3,196],[25.6,289],[27.0,177],[23.1,185],[16.4,51],[10.7,73],[3.3,25]],"36310":[[1.1,49],[3.3,49],[8.3,77],[13.4,91],[18.1,113],[22.9,183],[25.7,358],[26.6,279],[22.7,172],[15.4,45],[9.5,67],[2.0,29]],"34310":[[-0.3,37],[2.0,40],[7.5,64],[12.7,66],[17.5,95],[22.5,162],[25.2,355],[25.9,287],[21.6,171],[14.1,46],[7.9,57],[0.3,28]],"33390":[[-1.7,32],[0.9,33],[6.7,56],[12.6,55],[17.6,102],[22.7,139],[25.1,355],[25.5,293],[21.2,187],[13.3,45],[6.7,52],[-1.4,25]],"33340":[[-0.1,38],[2.2,38],[7.9,63],[13.0,68],[17.9,90],[23.0,145],[25.4,344],[26.1,278],[21.8,174],[14.4,46],[8.1,48],[0.4,29]],"31120":[[-2.6,30],[0.3,29],[6.1,50],[12.2,57],[17.0,123],[22.3,167],[25.4,331],[25.7,350],[21.5,170],[13.6,69],[6.5,67],[-1.8,21]],"31030":[[-3.2,31],[-0.3,27],[5.3,53],[11.3,60],[16.1,135],[21.5,195],[24.4,340],[24.6,380],[20.5,193],[12.5,81],[5.4,65],[-2.6,20]],"25040":[[-0.9,36],[1.6,32],[7.4,61],[13.0,55],[17.8,102],[22.9,123],[25.4,338],[26.0,273],[21.7,174],[14.1,43],[7.7,58],[-0.1,26]],"23070":[[-2.3,30],[0.4,32],[6.0,48],[12.0,58],[16.5,122],[21.7,169],[25.1,308],[25.5,351],[21.4,184],[13.5,71],[6.6,63],[-1.6,21]],"23060":[[-1.9,31],[0.5,31],[5.8,47],[11.6,57],[16.0,123],[21.1,180],[24.9,299],[25.5,357],[21.6,186],[13.9,67],[7.1,61],[-1.0,23]],"23040":[[-1.6,30],[0.9,33],[6.0,48],[11.8,57],[16.2,124],[21.3,174],[25.0,292],[25.6,352],[21.9,173],[14.2,61],[7.4,65],[-0.7,23]],"23020":[[-1.9,31],[0.5,31],[5.8,47],[11.6,57],[16.0,123],[21.1,180],[24.9,299],[25.5,357],[21.6,186],[13.9,67],[7.1,61],[-1.0,23]],"22040":[[0.7,36],[3.3,35],[8.9,68],[14.0,71],[18.9,91],[23.7,158],[25.9,285],[26.7,216],[22.1,182],[15.3,50],[9.2,47],[1.3,18]],"22030":[[0.9,32],[3.5,34],[9.1,58],[14.2,62],[19.0,88],[24.1,140],[26.1,264],[27.0,206],[22.4,162],[15.3,45],[9.2,45],[1.5,16]],"22010":[[0.8,35],[3.3,34],[9.0,65],[14.1,61],[19.0,95],[23.9,165],[26.0,307],[26.9,241],[22.3,185],[15.5,50],[9.2,45],[1.2,17]],"21150":[[2.5,58],[4.8,61],[9.4,100],[13.3,109],[17.3,125],[21.5,234],[24.9,309],[26.5,228],[22.8,228],[16.6,80],[10.6,56],[3.0,24]],"21130":[[1.7,60],[4.2,62],[8.9,102],[13.1,114],[17.5,127],[21.8,233],[25.0,336],[26.4,248],[22.4,226],[16.1,69],[9.9,51],[2.0,27]],"21100":[[2.5,58],[4.8,61],[9.4,100],[13.3,109],[17.3,125],[21.5,234],[24.9,309],[26.5,228],[22.8,228],[16.6,80],[10.6,56],[3.0,24]],"21070":[[3.3,61],[5.5,62],[9.8,101],[13.5,109],[17.4,122],[21.5,224],[24.9,323],[26.8,229],[23.3,224],[17.5,82],[11.5,55],[3.7,24]],"21050":[[1.7,60],[4.2,62],[8.9,102],[13.1,114],[17.5,127],[21.8,233],[25.0,336],[26.4,248],[22.4,226],[16.1,69],[9.9,51],[2.0,27]],"21030":[[3.3,61],[5.5,62],[9.8,101],[13.5,109],[17.4,122],[21.5,224],[24.9,323],[26.8,229],[23.3,224],[17.5,82],[11.5,55],[3.7,24]],"21020":[[2.5,58],[4.8,61],[9.4,100],[13.3,109],[17.3,125],[21.5,234],[24.9,309],[26.5,228],[22.8,228],[16.6,80],[10.6,56],[3.0,24]],"21010":[[2.5,58],[4.8,61],[9.4,100],[13.3,109],[17.3,125],[21.5,234],[24.9,309],[26.5,228],[22.8,228],[16.6,80],[10.6,56],[3.0,24]],"11210":[[-2.5,30],[0.2,34],[6.0,47],[11.8,58],[16.3,123],[21.5,175],[24.8,328],[25.1,371],[21.2,176],[13.5,62],[6.4,77],[-1.8,24]],"11190":[[-2.6,29],[0.2,31],[6.0,48],[11.9,57],[16.5,125],[21.8,165],[25.0,313],[25.3,368],[21.3,173],[13.5,68],[6.5,66],[-1.8,22]],"11170":[[-2.6,29],[0.2,31],[6.0,48],[11.9,57],[16.5,125],[21.8,165],[25.0,313],[25.3,368],[21.3,173],[13.5,68],[6.5,66],[-1.8,22]],"11130":[[-2.6,29],[0.2,30],[5.9,50],[11.9,58],[16.6,129],[21.9,176],[25.1,329],[25.4,364],[21.2,188],[13.4,76],[6.3,65],[-1.9,22]],"11110":[[-2.7,31],[0.1,28],[5.8,50],[11.8,58],[16.6,128],[22.0,188],[25.0,339],[25.2,366],[20.9,178],[12.9,83],[5.9,65],[-2.1,20]],"11090":[[-2.7,31],[0.1,30],[5.6,51],[11.7,59],[16.3,129],[21.7,179],[24.7,341],[25.0,381],[20.9,192],[13.0,88],[5.9,65],[-2.1,22]],"11070":[[-2.6,30],[0.3,29],[6.1,50],[12.2,57],[17.0,123],[22.3,167],[25.4,331],[25.7,350],[21.5,170],[13.6,69],[6.5,67],[-1.8,21]],"11050":[[-2.6,30],[0.3,29],[6.1,50],[12.2,57],[17.0,123],[22.3,167],[25.4,331],[25.7,350],[21.5,170],[13.6,69],[6.5,67],[-1.8,21]],"11040":[[-2.6,28],[0.2,32],[6.0,47],[11.9,59],[16.6,123],[21.9,174],[25.1,321],[25.4,367],[21.3,171],[13.5,61],[6.4,68],[-1.9,22]],"11030":[[-2.6,29],[0.2,30],[5.9,50],[11.9,58],[16.6,129],[21.9,176],[25.1,329],[25.4,364],[21.2,188],[13.4,76],[6.3,65],[-1.9,22]],"11020":[[-2.6,29],[0.2,30],[5.9,50],[11.9,58],[16.6,129],[21.9,176],[25.1,329],[25.4,364],[21.2,188],[13.4,76],[6.3,65],[-1.9,22]],"11010":[[-2.6,29],[0.2,30],[5.9,50],[11.9,58],[16.6,129],[21.9,176],[25.1,329],[25.4,364],[21.2,188],[13.4,76],[6.3,65],[-1.9,22]],"31041":[[-2.1,29],[0.5,33],[6.0,46],[11.6,57],[16.2,124],[21.3,175],[24.7,327],[25.1,370],[21.3,178],[13.7,59],[6.7,78],[-1.4,24]],"39020":[[6.6,73],[7.4,77],[10.6,113],[14.2,114],[18.0,165],[21.9,300],[25.5,358],[26.9,285],[23.9,288],[18.2,60],[13.4,105],[7.5,34]],"39010":[[5.6,103],[6.7,113],[9.9,138],[13.4,139],[17.5,184],[21.5,230],[25.5,271],[26.6,202],[23.2,384],[17.7,75],[12.6,122],[6.6,50]],"38400":[[1.4,41],[3.5,44],[8.9,66],[13.7,82],[18.6,107],[23.4,172],[25.8,293],[26.8,251],[22.4,180],[15.5,49],[9.2,51],[1.8,21]],"38390":[[0.7,42],[2.7,42],[7.9,62],[12.7,75],[17.5,92],[22.4,167],[24.9,305],[25.8,242],[21.4,180],[14.2,42],[8.1,54],[1.0,22]],"38380":[[0.7,45],[2.6,48],[7.7,66],[12.5,81],[17.4,96],[22.2,159],[24.9,293],[25.8,230],[21.3,184],[14.2,43],[8.2,59],[1.2,25]],"38370":[[1.9,54],[3.8,57],[8.8,92],[13.3,121],[17.9,154],[22.3,207],[25.0,312],[26.2,251],[22.1,197],[15.6,56],[9.7,74],[2.4,25]],"38360":[[2.3,51],[4.3,52],[9.1,87],[13.4,122],[17.9,132],[22.2,208],[25.2,311],[26.7,216],[22.8,206],[16.3,62],[10.3,64],[2.9,20]],"38350":[[3.1,50],[4.8,59],[9.2,84],[13.2,121],[17.4,138],[21.6,214],[24.9,291],[26.7,197],[23.1,275],[17.1,60],[11.3,58],[4.2,21]],"38340":[[3.3,50],[5.2,60],[9.8,97],[13.9,114],[18.1,131],[22.1,221],[25.4,291],[26.9,231],[23.2,252],[17.1,65],[11.4,59],[3.8,20]],"38330":[[1.4,40],[3.7,41],[8.9,84],[13.7,93],[18.5,103],[23.1,177],[25.4,314],[26.4,254],[22.2,224],[15.7,55],[9.5,56],[1.7,18]],"38320":[[1.5,39],[3.9,39],[9.2,80],[13.9,93],[18.7,103],[23.4,168],[25.7,276],[26.8,216],[22.4,229],[15.7,56],[9.6,57],[1.9,18]],"38310":[[1.3,41],[3.7,40],[8.9,81],[13.5,99],[18.3,106],[23.0,175],[25.4,286],[26.5,226],[22.1,221],[15.4,59],[9.3,59],[1.8,21]],"38100":[[2.3,63],[4.6,58],[9.3,105],[13.6,100],[18.0,147],[22.4,225],[25.5,348],[26.7,271],[22.8,232],[16.5,65],[10.5,52],[2.6,23]],"38090":[[4.1,57],[5.9,62],[10.0,95],[13.6,118],[17.5,121],[21.5,222],[24.8,296],[26.6,222],[23.2,238],[17.7,90],[12.1,56],[4.8,25]],"38080":[[2.2,49],[4.6,51],[9.5,91],[13.9,86],[18.5,118],[22.9,196],[25.8,312],[26.9,242],[22.8,216],[16.2,58],[10.2,52],[2.5,18]],"38070":[[1.2,45],[3.7,47],[8.8,86],[13.3,92],[18.1,120],[22.6,177],[25.3,282],[26.4,222],[22.1,237],[15.4,54],[9.3,54],[1.6,19]],"37430":[[4.1,64],[4.8,53],[8.0,62],[11.8,65],[15.8,88],[20.4,77],[23.8,153],[26.0,164],[22.3,149],[17.0,73],[12.3,72],[5.6,69]],"37420":[[1.3,46],[2.9,49],[7.5,69],[12.3,62],[17.1,98],[21.4,105],[24.2,260],[25.2,270],[20.8,227],[14.8,95],[9.5,47],[2.0,37]],"37410":[[-1.6,34],[0.7,35],[6.1,70],[11.2,69],[16.1,121],[21.1,139],[23.8,296],[24.4,264],[20.0,175],[13.0,47],[6.6,53],[-1.5,23]],"37400":[[-1.1,30],[1.4,31],[7.1,65],[12.6,65],[17.6,116],[22.7,152],[25.0,346],[25.5,305],[21.1,171],[13.7,43],[7.1,45],[-0.8,20]],"37390":[[0.8,32],[3.2,34],[8.9,59],[14.1,59],[18.9,96],[23.9,133],[25.9,275],[26.9,221],[22.3,168],[15.1,47],[8.8,50],[1.2,19]],"37380":[[1.0,38],[3.3,37],[8.7,62],[13.6,73],[18.4,95],[23.3,155],[25.6,278],[26.5,235],[22.0,184],[15.1,55],[8.8,47],[1.3,22]],"37370":[[1.3,36],[3.6,37],[9.0,61],[13.9,76],[18.7,95],[23.6,157],[25.9,272],[26.7,232],[22.3,167],[15.1,46],[8.9,45],[1.6,18]],"37360":[[1.4,45],[3.6,43],[8.7,87],[13.2,90],[17.9,119],[22.5,169],[25.2,305],[26.2,231],[22.0,206],[15.3,57],[9.2,57],[1.7,22]],"37350":[[1.1,43],[3.3,42],[8.4,72],[13.1,70],[17.8,110],[22.2,150],[25.1,258],[26.1,271],[21.5,254],[15.4,74],[9.5,66],[1.6,34]],"37340":[[-0.6,35],[1.6,36],[7.0,74],[11.9,66],[16.8,112],[21.6,143],[24.2,250],[25.0,247],[20.5,189],[13.9,51],[7.7,52],[-0.4,25]],"37330":[[-0.4,32],[1.9,30],[7.4,65],[12.4,60],[17.4,107],[22.3,136],[24.8,251],[25.6,232],[21.0,175],[14.1,49],[7.9,51],[-0.0,19]],"37310":[[0.2,28],[2.7,32],[8.7,63],[13.9,57],[18.9,98],[23.8,147],[26.0,288],[26.7,259],[22.1,163],[14.8,44],[8.3,44],[0.5,19]],"37100":[[0.4,39],[2.9,38],[8.3,74],[13.3,71],[18.1,103],[22.7,152],[25.2,286],[26.2,207],[21.5,189],[14.7,50],[8.5,50],[0.8,21]],"37090":[[-0.1,32],[2.3,33],[8.0,61],[13.3,66],[18.4,109],[23.5,143],[25.7,330],[26.3,263],[21.8,181],[14.5,33],[8.0,46],[0.1,21]],"37080":[[-0.8,32],[1.7,34],[7.5,60],[12.8,61],[17.8,104],[22.9,141],[25.1,339],[25.7,276],[21.2,170],[13.9,42],[7.5,40],[-0.4,26]],"37070":[[0.4,40],[2.8,35],[8.1,72],[13.1,61],[18.0,101],[22.7,136],[25.1,275],[26.0,215],[21.4,209],[14.6,54],[8.4,46],[0.7,21]],"37060":[[-0.9,30],[1.4,29],[7.0,64],[12.3,64],[17.4,116],[22.5,140],[24.9,307],[25.4,277],[20.8,166],[13.3,46],[6.8,45],[-0.8,21]],"37050":[[-0.1,27],[2.3,31],[8.3,60],[13.5,53],[18.5,94],[23.7,140],[25.8,304],[26.6,272],[22.0,166],[14.7,41],[8.2,43],[0.4,19]],"37040":[[-0.0,30],[2.5,29],[8.3,65],[13.5,58],[18.6,111],[23.6,145],[25.8,289],[26.5,263],[21.9,167],[14.7,45],[8.3,41],[0.4,19]],"37030":[[0.5,33],[2.8,35],[8.5,59],[13.8,56],[18.8,83],[24.0,119],[26.0,289],[26.8,265],[22.1,178],[14.8,39],[8.4,47],[0.8,23]],"37020":[[1.5,44],[3.9,39],[9.0,73],[13.7,71],[18.5,95],[23.1,134],[25.7,288],[26.7,216],[22.1,258],[15.6,64],[9.7,52],[1.9,25]],"36480":[[4.5,53],[4.7,46],[7.5,57],[11.6,78],[15.8,104],[20.1,149],[23.9,258],[26.0,122],[23.6,186],[18.2,42],[12.8,73],[6.5,45]],"36470":[[4.9,51],[5.5,46],[8.8,70],[12.6,85],[16.6,120],[20.9,194],[24.7,261],[26.7,99],[23.5,191],[17.8,53],[12.6,77],[6.4,30]],"36460":[[3.9,55],[5.1,54],[9.0,86],[12.9,116],[16.8,149],[20.9,221],[24.4,267],[26.5,135],[23.6,185],[17.5,43],[11.9,73],[5.2,23]],"36450":[[0.9,50],[3.0,54],[7.7,73],[12.8,92],[17.5,125],[22.3,181],[25.3,372],[26.3,241],[22.3,177],[15.1,45],[9.3,68],[1.9,27]],"36440":[[2.5,47],[3.9,47],[7.9,69],[12.6,80],[17.1,118],[21.9,175],[25.5,359],[26.8,211],[23.1,185],[16.4,47],[11.0,73],[4.1,35]],"36430":[[1.6,48],[3.5,52],[8.0,76],[12.8,91],[17.4,127],[22.3,185],[25.4,352],[26.5,214],[22.6,199],[15.6,47],[10.1,70],[2.8,29]],"36420":[[2.5,49],[4.0,47],[8.2,69],[12.9,84],[17.4,115],[22.2,175],[25.6,343],[27.0,208],[23.2,198],[16.4,49],[10.9,70],[3.9,29]],"36410":[[2.9,43],[4.3,47],[8.6,71],[13.2,89],[17.6,115],[22.4,201],[25.7,288],[27.1,153],[23.3,174],[16.5,51],[11.0,71],[4.1,23]],"36400":[[2.9,47],[4.3,52],[8.6,68],[13.0,103],[17.3,132],[21.8,207],[25.3,251],[26.6,151],[23.1,190],[16.4,49],[10.9,76],[3.9,24]],"36380":[[2.2,48],[4.0,51],[8.4,79],[12.9,116],[17.1,151],[21.5,199],[24.9,283],[26.4,193],[22.7,191],[16.2,51],[10.4,71],[3.1,23]],"36370":[[1.7,44],[3.7,52],[8.5,77],[13.1,99],[17.6,118],[22.2,200],[25.2,314],[26.4,217],[22.5,179],[15.7,46],[9.9,72],[2.6,22]],"36360":[[1.6,46],[3.6,50],[8.5,81],[13.1,102],[17.5,136],[22.1,200],[25.1,314],[26.3,238],[22.4,189],[15.6,49],[9.7,75],[2.3,21]],"36350":[[2.6,49],[4.5,51],[9.2,78],[13.5,124],[17.9,144],[22.4,225],[25.4,287],[27.0,204],[23.2,195],[16.7,59],[10.7,68],[3.3,22]],"36330":[[2.6,57],[4.5,59],[9.5,92],[14.2,107],[18.7,153],[23.2,262],[25.9,391],[27.1,293],[23.3,191],[16.5,54],[10.5,73],[3.2,23]],"36320":[[1.5,46],[3.6,51],[8.6,76],[13.5,91],[18.1,104],[22.8,200],[25.5,300],[26.6,206],[22.6,166],[15.4,54],[9.5,69],[2.2,21]],"36060":[[2.4,52],[4.4,57],[9.3,93],[13.7,130],[18.2,153],[22.6,227],[25.5,316],[26.9,225],[23.0,205],[16.4,61],[10.3,71],[2.8,19]],"36040":[[1.8,41],[3.8,49],[8.5,73],[13.4,89],[18.0,108],[22.9,186],[25.7,326],[26.8,197],[22.8,168],[15.7,46],[10.0,72],[2.7,23]],"36030":[[2.9,51],[4.7,58],[9.4,96],[13.9,129],[18.2,158],[22.6,236],[25.4,327],[26.9,255],[23.2,196],[16.8,50],[10.9,72],[3.6,21]],"36020":[[2.9,47],[4.7,61],[9.3,74],[13.5,107],[17.9,145],[22.1,203],[25.1,299],[26.9,216],[23.2,243],[17.0,60],[11.1,66],[4.0,18]],"36010":[[2.9,43],[4.3,47],[8.6,71],[13.2,89],[17.6,115],[22.4,201],[25.7,288],[27.1,153],[23.3,174],[16.5,51],[11.0,71],[4.1,23]],"35380":[[1.3,39],[3.1,50],[7.5,60],[12.4,63],[17.1,100],[22.0,143],[25.6,339],[26.8,235],[22.9,185],[15.8,46],[10.0,71],[2.8,46]],"35370":[[0.5,42],[2.7,48],[7.6,62],[12.7,64],[17.5,94],[22.2,143],[25.4,298],[26.2,229],[22.2,171],[14.9,46],[9.2,73],[1.9,40]],"35360":[[0.1,49],[2.3,49],[7.5,72],[12.5,90],[17.3,104],[22.0,188],[24.7,341],[25.7,285],[21.7,164],[14.5,43],[8.4,69],[0.8,27]],"35350":[[0.6,45],[2.7,47],[7.8,69],[13.0,85],[17.9,102],[22.7,178],[25.1,331],[26.1,279],[21.9,160],[14.4,45],[8.4,67],[1.2,24]],"35340":[[-0.6,49],[1.6,50],[7.1,76],[12.0,75],[16.7,102],[21.5,177],[24.2,318],[25.0,289],[21.0,177],[13.9,55],[7.7,72],[-0.1,35]],"35330":[[0.3,37],[2.5,39],[8.0,62],[13.0,63],[17.8,83],[22.8,136],[25.4,308],[26.1,263],[21.8,164],[14.4,45],[8.3,53],[0.9,28]],"35320":[[-0.3,42],[1.9,46],[7.2,69],[12.2,60],[16.9,106],[21.8,154],[24.7,336],[25.5,273],[21.3,162],[13.9,45],[7.7,62],[0.1,32]],"35310":[[0.8,47],[2.9,46],[8.1,63],[13.4,62],[18.2,114],[23.0,160],[25.7,402],[26.5,297],[22.7,169],[15.2,48],[9.1,65],[1.5,35]],"35060":[[0.8,41],[2.9,50],[7.9,59],[13.2,64],[18.0,95],[22.9,138],[25.9,349],[26.6,256],[22.5,179],[15.1,39],[9.3,63],[1.9,41]],"35050":[[0.6,43],[2.8,53],[8.0,74],[13.1,90],[17.9,99],[22.6,196],[25.0,341],[25.9,242],[21.8,172],[14.4,45],[8.5,72],[1.3,28]],"35040":[[0.6,39],[2.9,50],[7.9,61],[13.2,65],[18.1,94],[22.8,147],[25.7,329],[26.5,271],[22.4,173],[15.1,44],[9.2,70],[1.8,36]],"35030":[[0.2,39],[2.3,40],[7.6,63],[13.1,55],[17.9,105],[22.9,156],[25.7,394],[26.4,286],[22.3,171],[14.7,51],[8.6,66],[1.1,34]],"35020":[[1.0,38],[2.7,43],[7.2,59],[12.2,55],[16.8,113],[21.7,147],[25.4,386],[26.5,245],[22.7,194],[15.5,54],[9.7,71],[2.5,40]],"34380":[[0.3,32],[1.8,38],[6.2,41],[11.3,62],[15.7,116],[20.8,149],[24.8,270],[25.8,316],[22.3,165],[15.4,61],[9.4,72],[2.3,31]],"34370":[[-1.0,33],[1.2,38],[6.5,45],[12.1,58],[16.8,107],[22.0,146],[25.1,307],[25.6,298],[21.5,186],[14.0,47],[7.7,56],[0.0,26]],"34360":[[-0.2,35],[1.7,41],[6.4,46],[11.8,60],[16.3,114],[21.3,158],[24.9,311],[25.7,321],[22.1,174],[14.8,60],[8.8,62],[1.4,30]],"34350":[[-0.3,37],[1.9,33],[7.2,58],[12.7,56],[17.3,118],[22.5,131],[25.4,351],[26.1,277],[22.1,202],[14.5,41],[8.1,56],[0.5,27]],"34340":[[0.4,39],[2.4,41],[7.3,65],[12.5,58],[17.0,110],[22.0,164],[25.5,411],[26.4,266],[22.5,198],[15.1,52],[9.1,70],[1.7,38]],"34330":[[0.2,37],[2.3,38],[7.5,61],[13.0,55],[17.7,103],[22.7,151],[25.7,382],[26.5,273],[22.4,181],[14.8,44],[8.6,64],[1.2,32]],"34080":[[-0.5,31],[1.5,35],[6.5,39],[11.9,51],[16.6,107],[21.6,139],[25.2,281],[25.8,309],[22.0,174],[14.9,56],[8.5,67],[0.9,28]],"34070":[[-0.5,39],[1.8,43],[7.3,63],[12.8,65],[17.6,110],[22.6,155],[25.2,389],[26.0,288],[21.8,171],[14.3,45],[8.0,62],[0.2,29]],"34060":[[0.0,38],[2.2,37],[7.7,62],[13.2,57],[18.0,108],[23.0,157],[25.7,388],[26.4,296],[22.3,168],[14.7,48],[8.5,65],[0.9,33]],"34050":[[-0.7,36],[1.3,45],[6.3,50],[11.7,62],[16.3,119],[21.3,159],[24.8,329],[25.5,322],[21.7,190],[14.3,60],[8.1,61],[0.5,29]],"34040":[[-1.1,33],[1.4,35],[6.8,50],[12.6,53],[17.3,109],[22.5,128],[25.6,307],[26.0,344],[22.0,173],[14.2,57],[7.6,66],[-0.5,27]],"34030":[[0.5,37],[2.2,40],[6.7,50],[11.7,57],[16.1,113],[21.0,150],[24.9,359],[26.0,294],[22.4,212],[15.4,53],[9.4,68],[2.1,36]],"34020":[[-0.2,37],[2.2,33],[7.7,59],[13.2,56],[17.9,112],[23.1,129],[25.9,352],[26.6,273],[22.4,194],[14.8,44],[8.3,53],[0.5,26]],"33380":[[-1.3,29],[1.4,33],[7.1,60],[12.5,59],[17.4,119],[22.5,140],[25.2,354],[25.6,318],[21.2,189],[13.7,41],[7.1,55],[-1.2,25]],"33370":[[-2.3,29],[0.6,30],[6.4,55],[12.2,55],[17.1,111],[22.4,153],[25.0,330],[25.3,345],[21.1,188],[13.3,56],[6.4,55],[-2.0,26]],"33360":[[-0.9,32],[1.8,32],[7.6,55],[13.1,56],[18.0,97],[23.1,124],[25.7,334],[26.1,296],[21.9,179],[14.3,45],[7.7,53],[-0.7,27]],"33350":[[-1.6,31],[1.0,34],[6.8,56],[12.7,53],[17.7,102],[22.9,134],[25.3,345],[25.8,297],[21.4,185],[13.5,49],[6.8,54],[-1.3,25]],"33330":[[-0.9,36],[1.5,35],[7.3,61],[12.6,58],[17.5,97],[22.6,143],[25.1,357],[25.7,286],[21.5,184],[14.0,43],[7.5,53],[-0.4,23]],"33320":[[-1.1,38],[1.4,38],[7.1,60],[12.4,59],[17.3,101],[22.5,145],[24.9,359],[25.5,272],[21.2,182],[13.7,43],[7.2,52],[-0.7,26]],"33310":[[-1.4,32],[1.2,33],[6.9,56],[12.8,53],[17.7,105],[23.0,127],[25.4,342],[25.9,302],[21.5,188],[13.6,50],[7.0,55],[-1.1,25]],"33030":[[-2.2,29],[0.4,32],[6.0,56],[11.6,62],[16.6,119],[22.0,138],[24.8,356],[25.1,338],[20.8,185],[13.2,42],[6.5,58],[-1.6,26]],"33020":[[-1.4,30],[1.5,31],[7.2,52],[13.1,56],[18.0,110],[23.3,141],[25.8,343],[26.1,327],[21.8,186],[14.0,47],[7.2,48],[-1.0,25]],"32410":[[-0.2,66],[2.0,63],[7.2,70],[12.4,69],[17.3,119],[21.6,186],[24.7,239],[25.5,340],[20.8,293],[14.7,112],[8.9,49],[1.0,49]],"32400":[[-0.7,63],[1.6,61],[6.7,72],[12.1,73],[16.8,128],[21.1,176],[24.2,244],[25.1,319],[20.6,308],[14.6,117],[8.6,51],[0.6,48]],"32390":[[-3.5,34],[-1.1,30],[4.3,58],[9.6,57],[14.8,117],[20.0,166],[23.4,245],[23.5,307],[18.9,180],[11.4,67],[4.7,55],[-3.2,23]],"32050":[[-2.7,43],[-0.7,43],[4.7,73],[9.8,75],[14.7,119],[19.6,134],[22.5,303],[23.0,273],[18.4,203],[11.8,54],[5.6,55],[-2.4,31]],"32040":[[1.2,59],[3.0,56],[7.8,62],[12.7,64],[17.4,104],[21.6,135],[24.5,231],[25.5,319],[21.2,286],[15.2,118],[9.8,41],[2.0,45]],"32030":[[0.4,61],[2.3,55],[7.0,59],[12.1,63],[17.1,100],[21.5,132],[24.6,221],[25.6,335],[20.9,276],[14.6,106],[9.1,42],[1.5,42]],"32020":[[-1.6,31],[1.1,31],[6.9,54],[12.5,58],[17.3,114],[22.6,186],[25.4,313],[25.7,351],[21.3,180],[13.4,58],[6.6,54],[-1.4,23]],"32010":[[-2.7,29],[-0.1,25],[5.6,48],[11.3,57],[16.1,113],[21.6,169],[24.7,284],[24.8,378],[20.4,169],[12.4,63],[5.5,53],[-2.3,20]],"31380":[[-2.3,34],[0.5,29],[6.3,55],[12.2,61],[16.9,121],[22.1,190],[25.0,324],[25.2,397],[21.1,185],[13.2,55],[6.2,56],[-2.1,22]],"31370":[[-2.2,32],[0.6,30],[6.3,50],[12.1,66],[17.1,122],[22.4,173],[25.5,344],[25.7,395],[21.3,175],[13.5,82],[6.4,63],[-1.9,22]],"31350":[[-2.8,28],[-0.1,24],[5.5,41],[11.6,56],[16.5,130],[21.8,147],[25.0,293],[25.1,327],[20.9,195],[12.8,68],[5.7,59],[-2.4,19]],"31280":[[-2.1,34],[0.7,28],[6.5,53],[12.5,59],[17.3,114],[22.7,174],[25.4,316],[25.6,357],[21.3,183],[13.3,51],[6.3,53],[-1.8,22]],"31270":[[-3.3,30],[-0.5,28],[5.2,49],[11.1,61],[16.0,128],[21.3,171],[24.3,300],[24.4,346],[20.2,187],[12.4,65],[5.3,62],[-2.9,19]],"31260":[[-2.7,33],[0.2,30],[6.0,49],[11.8,59],[16.6,136],[22.0,187],[25.1,313],[25.1,359],[21.1,203],[13.3,70],[6.1,63],[-2.2,19]],"31250":[[-1.7,30],[1.1,31],[7.0,52],[12.8,60],[17.5,115],[22.7,170],[25.8,325],[26.0,381],[22.0,193],[14.2,59],[7.1,65],[-1.1,23]],"31240":[[-1.3,33],[1.1,36],[6.3,47],[11.9,56],[16.5,112],[21.5,153],[25.0,289],[25.6,343],[21.7,177],[14.2,60],[7.6,73],[-0.4,28]],"31230":[[-2.5,31],[0.2,28],[5.8,47],[11.7,61],[16.3,123],[21.5,166],[25.0,318],[25.3,349],[21.3,197],[13.4,71],[6.4,60],[-1.8,21]],"31220":[[-1.4,33],[1.3,34],[7.0,53],[12.8,55],[17.6,112],[22.9,144],[25.8,333],[26.1,341],[22.0,183],[14.2,49],[7.4,68],[-0.9,28]],"31210":[[-2.1,32],[0.6,32],[6.4,48],[12.3,57],[17.1,114],[22.4,165],[25.1,324],[25.4,345],[21.1,182],[13.1,44],[6.3,55],[-1.9,22]],"31200":[[-2.6,29],[0.2,28],[5.8,46],[11.9,60],[16.6,130],[21.9,167],[25.1,292],[25.3,341],[21.2,194],[13.2,65],[6.2,58],[-2.0,19]],"31180":[[-2.5,29],[0.4,31],[6.1,47],[12.1,55],[16.9,118],[22.1,164],[25.2,334],[25.5,366],[21.4,162],[13.5,65],[6.4,65],[-1.8,22]],"31170":[[-2.1,29],[0.5,33],[6.0,46],[11.6,57],[16.2,124],[21.3,175],[24.7,327],[25.1,370],[21.3,178],[13.7,59],[6.7,78],[-1.4,24]],"31160":[[-2.1,29],[0.5,33],[6.0,46],[11.6,57],[16.2,124],[21.3,175],[24.7,327],[25.1,370],[21.3,178],[13.7,59],[6.7,78],[-1.4,24]],"31150":[[-1.7,32],[0.8,32],[6.0,45],[11.7,57],[16.2,119],[21.3,164],[24.9,297],[25.4,344],[21.6,173],[14.0,60],[7.2,76],[-0.9,22]],"31140":[[-1.8,32],[0.9,34],[6.4,48],[12.3,56],[16.9,118],[22.2,172],[25.3,305],[25.7,349],[21.7,180],[13.8,59],[6.9,63],[-1.2,25]],"31130":[[-2.7,31],[0.1,28],[5.8,50],[11.8,58],[16.6,128],[22.0,188],[25.0,339],[25.2,366],[20.9,178],[12.9,83],[5.9,65],[-2.1,20]],"31110":[[-2.6,28],[0.2,32],[6.0,47],[11.9,59],[16.6,123],[21.9,174],[25.1,321],[25.4,367],[21.3,171],[13.5,61],[6.4,68],[-1.9,22]],"31080":[[-2.4,32],[0.5,26],[6.2,48],[12.1,58],[17.0,131],[22.2,167],[25.4,290],[25.4,327],[21.3,189],[13.4,70],[6.4,59],[-1.9,20]],"31070":[[-0.9,32],[1.5,36],[7.0,47],[12.7,54],[17.4,106],[22.6,122],[25.8,295],[26.2,318],[22.2,172],[14.5,49],[7.9,60],[-0.1,30]],"31060":[[-2.2,29],[0.5,32],[6.0,45],[11.7,57],[16.3,131],[21.4,175],[24.9,309],[25.2,373],[21.4,174],[13.7,64],[6.7,73],[-1.4,21]],"29010":[[-0.8,36],[1.6,32],[7.3,60],[12.9,54],[17.6,107],[22.9,122],[25.4,335],[26.1,262],[21.8,182],[14.1,42],[7.7,54],[-0.1,25]],"26310":[[2.8,64],[5.0,61],[9.5,96],[13.4,107],[17.5,120],[21.7,188],[25.1,280],[26.5,240],[22.7,234],[16.7,77],[10.8,48],[3.1,29]],"26040":[[2.5,60],[4.7,53],[9.1,91],[13.2,88],[17.3,108],[21.7,167],[25.0,296],[26.5,236],[22.7,212],[16.6,70],[10.5,49],[3.0,25]],"26030":[[2.7,60],[4.9,53],[9.3,91],[13.3,88],[17.4,108],[21.8,167],[25.2,296],[26.6,236],[22.8,212],[16.8,70],[10.7,49],[3.1,25]],"26020":[[2.5,60],[4.7,53],[9.1,91],[13.2,88],[17.3,108],[21.7,167],[25.0,296],[26.5,236],[22.7,212],[16.6,70],[10.5,49],[3.0,25]],"26010":[[1.5,58],[4.0,51],[8.6,87],[13.1,88],[17.5,110],[22.1,174],[25.3,300],[26.4,239],[22.0,226],[15.6,69],[9.4,49],[1.8,24]],"25050":[[-0.9,34],[1.6,32],[7.4,61],[13.0,58],[17.9,96],[23.0,122],[25.4,331],[26.0,276],[21.8,167],[14.1,42],[7.7,53],[-0.1,24]],"25030":[[-0.3,34],[2.2,32],[8.0,61],[13.6,58],[18.5,96],[23.6,122],[26.0,331],[26.6,276],[22.4,167],[14.7,42],[8.4,53],[0.5,24]],"25020":[[-0.9,35],[1.5,35],[7.2,60],[12.8,55],[17.6,96],[22.7,132],[25.2,344],[25.8,280],[21.5,168],[13.8,46],[7.6,53],[-0.1,25]],"25010":[[-0.4,34],[2.1,34],[7.9,61],[13.3,57],[18.2,96],[23.3,136],[25.8,348],[26.5,290],[22.3,178],[14.8,46],[8.3,52],[0.4,24]],"24050":[[1.8,41],[3.8,49],[8.5,73],[13.4,89],[18.0,108],[22.9,186],[25.7,326],[26.8,197],[22.8,168],[15.7,46],[10.0,72],[2.7,23]],"24040":[[1.0,45],[3.2,49],[8.1,76],[13.1,91],[17.9,113],[22.8,186],[25.5,343],[26.4,222],[22.4,168],[15.1,42],[9.4,68],[2.0,22]],"24030":[[1.8,41],[3.8,49],[8.5,73],[13.4,89],[18.0,108],[22.9,186],[25.7,326],[26.8,197],[22.8,168],[15.7,46],[10.0,72],[2.7,23]],"24020":[[1.4,43],[3.5,48],[8.3,70],[13.2,90],[18.0,117],[22.8,186],[25.6,335],[26.6,212],[22.6,163],[15.4,43],[9.7,69],[2.3,23]],"24010":[[0.9,45],[3.1,54],[8.0,73],[12.9,90],[17.6,113],[22.4,190],[25.2,341],[26.1,230],[22.2,169],[15.0,44],[9.3,75],[1.8,23]],"23320":[[1.0,35],[1.9,34],[5.1,38],[9.1,56],[13.1,99],[17.6,140],[22.2,239],[24.8,280],[22.9,172],[17.0,52],[10.8,69],[3.2,33]],"23310":[[-1.7,31],[0.6,31],[5.8,46],[11.4,62],[15.8,124],[20.8,180],[24.8,290],[25.6,331],[22.0,197],[14.7,67],[7.7,58],[-0.5,21]],"23080":[[-1.9,31],[0.5,31],[5.8,47],[11.6,57],[16.0,123],[21.1,180],[24.9,299],[25.5,357],[21.6,186],[13.9,67],[7.1,61],[-1.0,23]],"23050":[[-1.6,30],[0.9,33],[6.0,48],[11.8,57],[16.2,124],[21.3,174],[25.0,292],[25.6,352],[21.9,173],[14.2,61],[7.4,65],[-0.7,23]],"23030":[[-1.9,31],[0.6,31],[5.8,47],[11.6,57],[16.0,123],[21.2,180],[24.9,299],[25.6,357],[21.7,186],[13.9,67],[7.1,61],[-1.0,23]],"23010":[[0.1,34],[1.4,30],[5.3,48],[10.2,57],[14.5,108],[19.3,142],[23.7,250],[25.6,302],[23.0,185],[16.5,60],[9.9,60],[2.0,26]],"22310":[[1.8,37],[4.2,38],[9.5,67],[14.5,80],[19.3,98],[24.0,165],[26.2,286],[27.0,251],[22.7,186],[16.0,49],[9.9,51],[2.3,18]],"22070":[[1.0,32],[3.5,34],[9.2,58],[14.3,62],[19.1,88],[24.1,140],[26.2,264],[27.1,206],[22.4,162],[15.4,45],[9.2,45],[1.5,16]],"22060":[[0.9,37],[3.3,36],[8.8,71],[13.9,64],[18.8,102],[23.5,165],[25.8,307],[26.7,232],[22.1,192],[15.3,50],[9.0,48],[1.2,19]],"22050":[[0.8,32],[3.4,34],[9.0,58],[14.1,62],[18.9,88],[24.0,140],[26.0,264],[26.9,206],[22.3,162],[15.3,45],[9.1,45],[1.4,16]],"22020":[[0.8,37],[3.3,36],[8.8,71],[13.9,64],[18.7,102],[23.5,165],[25.8,307],[26.7,232],[22.1,192],[15.3,50],[9.0,48],[1.2,19]],"21310":[[2.3,66],[4.5,62],[9.0,105],[12.9,119],[17.0,128],[21.2,212],[24.5,296],[26.0,243],[22.2,246],[16.3,77],[10.4,50],[2.6,31]],"21140":[[2.8,63],[5.0,63],[9.3,104],[13.2,117],[17.2,127],[21.4,227],[24.7,330],[26.4,251],[22.8,232],[17.0,77],[11.0,54],[3.1,26]],"21120":[[1.5,56],[3.9,60],[8.8,97],[13.0,105],[17.5,126],[21.8,227],[25.0,300],[26.3,243],[22.2,223],[15.6,70],[9.4,51],[1.8,25]],"21110":[[1.8,62],[4.2,63],[8.9,108],[13.2,114],[17.6,140],[22.0,234],[25.2,352],[26.5,269],[22.5,238],[16.2,68],[10.0,52],[2.1,27]],"21090":[[2.8,63],[5.0,63],[9.3,104],[13.2,117],[17.2,127],[21.4,227],[24.7,330],[26.4,251],[22.8,232],[17.0,77],[11.0,54],[3.1,26]],"21080":[[1.1,60],[3.5,62],[8.2,102],[12.4,114],[16.8,127],[21.1,233],[24.3,336],[25.7,248],[21.8,226],[15.5,69],[9.2,51],[1.3,27]],"21060":[[2.8,63],[5.0,63],[9.3,104],[13.2,117],[17.2,127],[21.4,227],[24.7,330],[26.4,251],[22.8,232],[17.0,77],[11.0,54],[3.1,26]],"21040":[[2.4,61],[4.6,62],[8.9,101],[12.6,109],[16.5,122],[20.6,224],[24.0,323],[25.9,229],[22.4,224],[16.6,82],[10.6,55],[2.8,24]],"11250":[[-2.5,29],[0.4,31],[6.1,47],[12.1,55],[16.9,118],[22.1,164],[25.2,334],[25.5,366],[21.4,162],[13.5,65],[6.4,65],[-1.8,22]],"11240":[[-2.6,28],[0.2,32],[6.0,47],[11.9,59],[16.6,123],[21.9,174],[25.1,321],[25.4,367],[21.3,171],[13.5,61],[6.4,68],[-1.9,22]],"11230":[[-2.6,29],[0.2,33],[6.0,45],[11.8,59],[16.5,114],[21.8,174],[24.9,328],[25.1,367],[21.1,186],[13.4,59],[6.3,75],[-1.9,23]],"11220":[[-2.6,28],[0.2,32],[6.0,47],[11.9,59],[16.6,123],[21.9,174],[25.1,321],[25.4,367],[21.3,171],[13.5,61],[6.4,68],[-1.9,22]],"11200":[[-2.6,29],[0.2,31],[6.0,48],[11.9,57],[16.5,125],[21.8,165],[25.0,313],[25.3,368],[21.3,173],[13.5,68],[6.5,66],[-1.8,22]],"11180":[[-2.6,29],[0.2,31],[6.0,48],[11.9,57],[16.5,125],[21.8,165],[25.0,313],[25.3,368],[21.3,173],[13.5,68],[6.5,66],[-1.8,22]],"11160":[[-2.3,29],[0.5,31],[6.0,47],[11.9,54],[16.5,128],[21.6,173],[25.1,301],[25.5,368],[21.5,180],[13.6,69],[6.7,63],[-1.5,20]],"11150":[[-2.5,29],[0.3,28],[6.0,46],[12.0,57],[16.7,128],[21.9,169],[25.1,319],[25.4,362],[21.3,177],[13.5,72],[6.5,63],[-1.8,20]],"11140":[[-2.5,29],[0.3,28],[6.0,46],[12.0,57],[16.7,128],[21.9,169],[25.1,319],[25.4,362],[21.3,177],[13.5,72],[6.5,63],[-1.8,20]],"11120":[[-2.6,29],[0.2,30],[5.9,50],[11.9,58],[16.6,129],[21.9,176],[25.1,329],[25.4,364],[21.2,188],[13.4,76],[6.3,65],[-1.9,22]],"11100":[[-2.7,31],[0.1,30],[5.6,51],[11.7,59],[16.3,129],[21.7,179],[24.7,341],[25.0,381],[20.9,192],[13.0,88],[5.9,65],[-2.1,22]],"11080":[[-2.6,30],[0.3,29],[6.1,50],[12.2,57],[17.0,123],[22.3,167],[25.4,331],[25.7,350],[21.5,170],[13.6,69],[6.5,67],[-1.8,21]],"11060":[[-2.6,30],[0.3,29],[6.1,50],[12.2,57],[17.0,123],[22.3,167],[25.4,331],[25.7,350],[21.5,170],[13.6,69],[6.5,67],[-1.8,21]],"38111":[[2.4,53],[4.6,53],[9.4,95],[13.7,107],[18.2,135],[22.4,196],[25.5,280],[26.9,220],[22.8,261],[16.4,64],[10.4,55],[2.8,21]],"37011":[[1.7,53],[4.0,44],[8.8,75],[13.2,79],[17.7,103],[22.1,137],[25.1,285],[26.3,224],[22.2,254],[16.1,75],[10.1,53],[2.1,28]],"35011":[[0.6,41],[2.6,46],[7.7,62],[13.0,67],[18.0,105],[22.9,151],[25.7,403],[26.5,283],[22.4,168],[14.8,48],[8.9,65],[1.5,36]],"34011":[[-1.6,34],[1.0,37],[6.6,48],[12.3,53],[17.2,111],[22.4,128],[25.2,322],[25.6,304],[21.6,186],[14.0,55],[7.2,61],[-1.0,27]],"33011":[[-1.4,32],[1.2,33],[6.9,56],[12.8,53],[17.7,105],[23.0,127],[25.4,342],[25.9,302],[21.5,188],[13.6,50],[7.0,55],[-1.1,25]],"31191":[[-2.2,31],[0.5,33],[6.0,48],[11.8,59],[16.4,114],[21.7,176],[24.7,331],[25.0,381],[21.1,182],[13.4,64],[6.5,76],[-1.5,23]],"31101":[[-2.5,29],[0.3,28],[6.0,46],[12.0,57],[16.7,128],[21.9,169],[25.1,319],[25.4,362],[21.3,177],[13.5,72],[6.5,63],[-1.8,20]],"31091":[[-0.7,30],[1.5,34],[6.5,40],[12.0,57],[16.4,114],[21.4,152],[25.1,277],[25.9,313],[22.2,170],[15.0,54],[8.4,74],[0.5,27]],"31051":[[-2.3,29],[0.5,31],[6.0,47],[11.9,54],[16.5,128],[21.6,173],[25.1,301],[25.5,368],[21.5,180],[13.6,69],[6.7,63],[-1.5,20]],"31021":[[-1.9,29],[0.9,33],[6.7,45],[12.5,59],[17.2,114],[22.5,174],[25.5,328],[25.8,367],[21.8,186],[14.0,59],[6.9,75],[-1.2,23]],"31011":[[-2.1,30],[0.5,32],[6.0,47],[11.7,58],[16.4,119],[21.6,169],[24.9,314],[25.3,362],[21.3,187],[13.6,63],[6.6,72],[-1.4,25]],"32380":[[-2.8,29],[-0.2,23],[5.3,52],[10.8,62],[16.0,116],[21.4,157],[24.6,254],[24.7,315],[20.1,174],[12.4,71],[5.6,53],[-2.4,20]],"32370":[[-2.2,28],[0.4,23],[5.8,49],[11.3,57],[16.4,118],[21.7,161],[25.0,255],[25.0,272],[20.5,174],[13.0,68],[6.0,54],[-1.9,20]],"32360":[[-4.2,29],[-1.3,24],[4.2,49],[10.1,57],[15.2,137],[20.5,160],[23.7,268],[23.7,305],[19.3,192],[11.4,83],[4.4,66],[-3.9,21]],"32350":[[-2.2,37],[0.2,38],[5.6,60],[10.8,64],[15.7,99],[20.9,133],[23.9,288],[24.2,289],[19.7,169],[12.6,47],[5.9,49],[-2.1,26]],"32340":[[-2.8,30],[-0.3,33],[5.3,57],[10.5,63],[15.4,105],[20.6,159],[23.6,309],[23.9,341],[19.6,175],[12.1,48],[5.4,54],[-2.7,26]],"32330":[[-1.6,28],[1.0,31],[6.6,59],[12.0,60],[17.0,112],[22.1,137],[24.9,335],[25.2,315],[20.8,178],[13.2,41],[6.6,51],[-1.6,22]],"32320":[[-2.5,32],[0.1,32],[5.8,53],[11.4,59],[16.2,120],[21.4,181],[24.4,303],[24.6,361],[20.2,175],[12.4,61],[5.6,55],[-2.4,22]],"32310":[[-2.2,34],[0.5,29],[6.3,56],[11.8,68],[16.8,122],[22.1,195],[25.1,302],[25.3,392],[20.9,196],[13.1,65],[6.2,58],[-1.9,22]],"32070":[[0.3,59],[2.0,68],[6.9,68],[12.0,70],[16.9,91],[21.4,129],[24.2,253],[25.0,321],[20.4,290],[14.2,112],[8.6,47],[0.9,46]],"32060":[[-0.3,63],[2.0,61],[7.2,72],[12.5,73],[17.2,128],[21.5,176],[24.6,244],[25.5,319],[21.0,308],[15.0,117],[9.0,51],[1.0,48]],"91001":[[-6.0,51],[-5.0,60],[0.7,87],[6.5,110],[11.6,108],[16.2,140],[20.6,114],[21.9,208],[18.0,161],[11.2,142],[4.8,118],[-3.7,42]],"92002":[[-1.4,114],[-0.9,94],[3.4,102],[8.5,96],[13.5,99],[17.8,129],[21.7,175],[23.4,254],[19.7,125],[13.4,118],[7.6,118],[0.4,119]],"92003":[[-2.2,79],[-1.3,76],[3.3,95],[8.7,102],[13.9,99],[18.0,129],[21.7,219],[23.4,269],[19.3,168],[12.7,111],[7.0,65],[-0.4,69]],"92004":[[1.7,52],[2.5,56],[6.3,110],[10.8,112],[15.3,110],[19.6,111],[23.3,208],[25.3,135],[22.0,141],[15.5,99],[10.3,62],[3.7,43]],"92005":[[-1.0,139],[-0.3,121],[4.0,139],[9.2,129],[14.3,175],[19.3,200],[23.2,369],[24.9,268],[20.9,200],[13.4,140],[7.8,195],[1.0,173]],"92006":[[0.1,185],[0.6,133],[4.2,142],[9.5,135],[14.6,141],[19.5,160],[23.3,317],[25.0,181],[21.1,185],[13.8,124],[8.4,199],[2.0,262]],"92007":[[2.3,49],[3.3,56],[7.4,137],[12.0,145],[16.7,171],[20.9,215],[24.8,249],[26.0,191],[22.4,190],[15.7,127],[10.8,86],[4.2,40]],"93008":[[6.1,68],[7.2,86],[10.7,143],[14.3,153],[18.3,172],[21.7,217],[25.5,211],[27.1,151],[24.4,177],[18.8,215],[14.5,98],[8.2,67]],"93009":[[3.7,34],[5.1,44],[9.7,106],[14.0,106],[18.5,139],[22.6,227],[26.3,229],[27.6,175],[23.9,190],[16.9,142],[11.7,75],[5.5,24]],"93010":[[3.7,38],[5.2,49],[9.7,115],[14.1,119],[18.5,146],[22.6,241],[26.2,225],[27.5,187],[23.9,196],[17.0,157],[11.7,75],[5.5,29]],"93011":[[4.3,58],[5.8,62],[10.1,145],[14.2,145],[18.6,159],[22.5,211],[26.6,194],[28.0,140],[24.3,176],[17.5,160],[12.5,89],[6.3,41]],"93012":[[4.3,58],[5.7,56],[10.0,134],[14.1,148],[18.5,162],[22.2,213],[26.2,198],[27.7,136],[24.2,169],[17.4,160],[12.5,92],[6.2,39]],"93013":[[4.9,57],[6.3,63],[10.3,147],[14.3,167],[18.6,172],[22.3,210],[26.4,205],[27.9,138],[24.5,180],[17.9,156],[13.2,89],[7.0,43]],"93014":[[4.9,57],[6.3,63],[10.3,147],[14.3,167],[18.6,172],[22.3,210],[26.4,205],[27.9,138],[24.5,180],[17.9,156],[13.2,89],[7.0,43]],"94015":[[1.0,219],[1.7,171],[5.6,164],[10.4,145],[15.6,164],[20.3,210],[24.1,355],[26.0,204],[22.3,207],[14.9,172],[9.7,227],[3.2,312]],"94016":[[3.0,175],[4.0,133],[7.7,137],[11.8,158],[16.7,186],[21.6,190],[25.4,288],[27.3,194],[23.8,202],[16.5,137],[11.4,175],[4.9,253]],"94017":[[3.8,273],[4.4,194],[7.8,160],[12.0,168],[16.6,158],[21.4,192],[25.3,308],[27.2,183],[23.9,178],[16.7,150],[11.8,246],[6.0,361]],"94018":[[4.7,171],[5.3,130],[8.5,146],[12.6,184],[17.1,177],[21.9,234],[25.6,353],[27.5,211],[24.4,167],[17.5,141],[12.6,190],[6.8,237]],"94019":[[3.6,57],[5.0,82],[8.9,148],[13.2,144],[17.6,183],[21.4,271],[25.1,269],[26.4,165],[23.3,129],[16.3,140],[11.4,84],[5.6,35]],"94020":[[-1.8,122],[-0.6,117],[3.8,136],[9.0,132],[13.9,177],[18.4,191],[22.2,303],[23.8,138],[20.2,165],[12.3,131],[7.1,122],[0.4,131]],"94021":[[4.5,61],[5.5,79],[9.4,157],[13.8,176],[17.8,231],[22.4,290],[26.2,347],[27.5,281],[24.5,204],[17.4,143],[12.3,100],[6.3,61]],"94022":[[6.7,72],[7.7,117],[11.3,199],[15.0,206],[18.8,253],[22.4,334],[26.2,337],[27.7,226],[25.1,247],[19.2,203],[14.6,135],[8.5,59]],"94023":[[6.8,57],[7.6,91],[11.2,192],[15.0,178],[18.8,233],[22.6,321],[26.6,303],[28.0,188],[25.4,231],[19.2,202],[14.6,122],[8.6,56]],"95024":[[4.6,64],[5.5,74],[9.5,168],[13.9,201],[18.0,241],[22.3,321],[26.3,298],[27.5,304],[24.4,248],[17.7,169],[12.6,97],[6.2,42]],"95025":[[5.1,95],[5.5,96],[8.7,141],[13.0,192],[17.4,211],[22.1,258],[26.2,304],[27.6,211],[24.9,193],[18.4,146],[13.3,105],[7.2,108]],"95026":[[3.8,56],[4.8,73],[8.9,129],[13.4,164],[17.7,202],[22.2,261],[26.1,288],[27.6,191],[24.5,168],[17.1,144],[11.9,98],[5.5,39]],"95027":[[5.2,53],[6.0,72],[9.8,128],[14.4,166],[18.6,216],[23.0,260],[27.2,263],[29.0,190],[25.8,195],[18.4,156],[13.0,103],[7.0,32]],"95028":[[4.6,45],[5.3,58],[9.2,121],[13.5,153],[17.7,184],[22.1,217],[26.2,251],[27.9,202],[24.9,145],[17.9,121],[12.5,95],[6.2,23]],"95029":[[3.3,57],[4.3,73],[8.4,130],[12.8,171],[17.1,221],[21.6,272],[25.6,274],[27.2,198],[24.0,193],[16.6,154],[11.3,109],[5.0,35]],"95030":[[5.2,72],[6.1,93],[10.3,143],[14.6,163],[18.7,219],[22.8,274],[26.8,236],[28.2,204],[24.9,190],[18.1,160],[13.1,108],[7.0,59]],"96031":[[6.4,97],[6.8,89],[9.9,128],[13.5,155],[17.9,150],[22.5,191],[26.4,316],[28.0,237],[24.6,165],[18.2,105],[13.4,121],[7.9,95]],"96032":[[6.1,101],[6.5,94],[9.7,134],[13.4,140],[17.8,151],[22.4,195],[26.2,313],[27.8,230],[24.4,167],[17.9,94],[13.2,119],[7.8,100]],"96033":[[4.7,40],[5.5,57],[9.6,103],[13.9,156],[18.3,156],[22.5,213],[26.5,221],[28.4,183],[25.1,124],[18.0,79],[12.5,77],[6.2,23]],"96034":[[5.9,59],[6.7,86],[10.7,136],[14.8,172],[19.0,169],[23.1,240],[26.7,301],[28.6,233],[25.4,169],[18.8,76],[13.7,91],[7.4,33]],"96035":[[5.8,72],[6.8,91],[10.5,138],[14.2,140],[18.3,186],[22.4,304],[26.1,332],[27.8,296],[24.7,197],[18.5,68],[13.4,86],[6.9,36]],"97036":[[5.6,60],[6.5,71],[10.6,128],[14.9,143],[19.1,207],[23.3,228],[27.2,205],[28.5,177],[25.2,152],[18.5,167],[13.4,102],[7.1,34]],"97037":[[6.8,48],[7.0,63],[10.6,112],[14.5,130],[18.6,166],[22.7,220],[26.8,183],[28.9,150],[26.0,122],[19.5,96],[14.4,86],[8.6,31]],"97038":[[6.5,80],[7.1,88],[10.9,146],[14.7,157],[18.9,196],[23.0,265],[27.1,211],[28.4,222],[25.3,216],[18.9,139],[14.0,103],[7.9,61]],"97039":[[6.4,77],[7.3,98],[11.1,202],[14.5,228],[18.2,316],[21.8,369],[25.4,392],[26.6,455],[24.5,258],[18.8,155],[13.8,126],[7.4,33]],"98040":[[5.9,87],[7.0,96],[10.8,132],[14.3,124],[18.7,171],[23.0,272],[26.7,334],[28.0,307],[24.8,226],[18.4,66],[13.3,103],[7.0,49]],"98041":[[6.2,67],[7.5,90],[11.2,158],[14.9,146],[19.0,202],[23.2,317],[26.7,376],[27.9,327],[25.2,173],[18.6,67],[13.4,90],[6.9,36]],"98042":[[7.1,75],[8.4,113],[12.0,196],[15.7,168],[19.6,259],[23.4,386],[26.8,431],[28.1,329],[25.6,184],[19.7,71],[14.6,104],[8.0,45]],"98043":[[6.2,83],[7.9,134],[11.8,194],[15.4,164],[19.5,284],[23.2,407],[26.6,356],[27.6,324],[25.0,202],[18.6,109],[13.4,136],[6.5,73]],"98044":[[6.9,76],[7.7,99],[11.0,137],[14.7,130],[18.8,191],[23.0,266],[26.8,288],[28.3,230],[25.3,192],[19.2,68],[14.2,70],[8.1,37]],"98045":[[7.0,63],[8.7,133],[12.2,175],[15.4,188],[19.1,263],[22.5,458],[26.1,266],[27.0,274],[24.4,281],[19.0,112],[14.1,91],[7.6,43]],"98046":[[7.9,75],[9.5,165],[12.6,227],[15.9,233],[19.6,319],[23.0,592],[26.5,339],[27.5,267],[25.3,213],[19.9,94],[14.9,128],[8.4,65]],"99047":[[16.4,89],[17.2,97],[18.6,155],[20.6,140],[23.2,366],[26.0,368],[28.0,188],[27.8,303],[27.0,191],[24.7,129],[21.7,114],[17.7,158]]};   // 지역 코드 → [[월평균기온, 월강수량], x12]
const FESTIVALS = [{"n":"화천 산천어축제","c":"32370","m":[1],"t":"겨울","d":"얼음벌판 위 산천어 맨손잡기","s":"20270109","e":"20270131","p":"festival_photos/화천_산천어축제.jpg","cr":"Republic of  Korea from Seoul, Republic of Korea / CC BY-SA 2.0"},{"n":"인제 빙어축제","c":"32390","m":[1],"t":"겨울","d":"소양호 얼음낚시"},{"n":"태백산 눈축제","c":"32050","m":[1,2],"t":"겨울","d":"눈조각과 설경 산행","p":"festival_photos/태백산_눈축제.jpg","cr":"칼빈500 / CC BY-SA 3.0"},{"n":"영덕 대게축제","c":"37350","m":[2,3],"t":"먹거리","d":"제철 대게"},{"n":"울진 대게와 붉은대게축제","c":"37420","m":[2,3],"t":"먹거리","d":"동해안 대게 한상","s":"20260227","e":"20260302","p":"festival_photos/울진_대게와_붉은대게축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"광양 매화축제","c":"36060","m":[3],"t":"꽃","d":"섬진강변 매화 군락"},{"n":"구례 산수유꽃축제","c":"36330","m":[3],"t":"꽃","d":"노란 산수유 마을","s":"20260314","e":"20260322","p":"festival_photos/구례_산수유꽃축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"제주 들불축제","c":"39010","m":[3],"t":"봄","d":"새별오름 오름 불놓기","s":"20260309","e":"20260314","p":"festival_photos/제주_들불축제.png","cr":"한국관광공사 / CC BY-SA 4.0"},{"n":"청도 소싸움축제","c":"37360","m":[3,4],"t":"전통","d":"전통 소싸움"},{"n":"통영 국제음악제","c":"38050","m":[3,4],"t":"음악","d":"바다를 낀 현대음악 축제","p":"festival_photos/통영_국제음악제.jpg","cr":"Georgio / CC BY 2.0"},{"n":"진해 군항제","c":"38111","m":[3,4],"t":"벚꽃","d":"국내 최대 벚꽃 축제","s":"20260327","e":"20260405","p":"festival_photos/진해_군항제.JPG","cr":"hyolee2 / CC BY-SA 4.0"},{"n":"태안 세계튤립축제","c":"34380","m":[4],"t":"꽃","d":"바다 옆 튤립 정원"},{"n":"고창 청보리밭축제","c":"35370","m":[4,5],"t":"꽃","d":"초록 보리밭 물결","s":"20260418","e":"20260510","p":"festival_photos/고창_청보리밭축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"함평 나비대축제","c":"36430","m":[4,5],"t":"가족","d":"나비와 봄꽃","s":"20260424","e":"20260505","p":"festival_photos/함평_나비대축제.JPG","cr":"myself (User:Piotrus) / CC BY-SA 3.0"},{"n":"담양 대나무축제","c":"36310","m":[5],"t":"자연","d":"죽녹원 대숲","s":"20260501","e":"20260505","p":"festival_photos/담양_대나무축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"보성 다향대축제","c":"36360","m":[5],"t":"자연","d":"녹차밭 다원","s":"20260501","e":"20260505","p":"festival_photos/보성_다향대축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"남원 춘향제","c":"35050","m":[4,5],"t":"전통","d":"춘향전의 고장","s":"20260430","e":"20260506","p":"festival_photos/남원_춘향제.jpg","cr":"Shamus7792003 (talk) (Uploads) / Public domain"},{"n":"춘천 마임축제","c":"32010","m":[5],"t":"공연","d":"거리 마임과 퍼포먼스","s":"20260524","e":"20260531","p":"festival_photos/춘천_마임축제.jpg","cr":"춘천마임축제 / Public domain"},{"n":"하동 야생차문화축제","c":"38360","m":[5],"t":"자연","d":"지리산 야생차","s":"20260501","e":"20260505","p":"festival_photos/하동_야생차문화축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"여수 거북선축제","c":"36020","m":[5],"t":"전통","d":"이순신과 바다","s":"20260501","e":"20260503","p":"festival_photos/여수_거북선축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"강릉 단오제","c":"32030","m":[6],"t":"전통","d":"유네스코 무형유산 단오굿","s":"20260615","e":"20260622","p":"festival_photos/강릉_단오제.jpg","cr":"Korea.net / Korean Culture and Information Service (Jeon Han) / CC BY-SA 2.0"},{"n":"보령 머드축제","c":"34030","m":[7,8],"t":"여름","d":"대천해수욕장 갯벌 머드","s":"20260724","e":"20260809","p":"festival_photos/보령_머드축제.jpg","cr":"Hypnotica Studios Infinite from Toms River, New Jersey, USA / CC BY 2.0"},{"n":"대구 치맥페스티벌","c":"22070","m":[7],"t":"먹거리","d":"두류공원 치킨과 맥주","s":"20260701","e":"20260705","p":"festival_photos/대구_치맥페스티벌.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"정남진 장흥 물축제","c":"36380","m":[7,8],"t":"여름","d":"탐진강 물놀이","s":"20260725","e":"20260802","p":"festival_photos/정남진_장흥_물축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"봉화 은어축제","c":"37410","m":[7,8],"t":"여름","d":"내성천 은어잡이","s":"20260725","e":"20260802","p":"festival_photos/봉화_은어축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"평창 대관령음악제","c":"32340","m":[7,8],"t":"음악","d":"고원의 클래식","s":"20260723","e":"20260802","p":"festival_photos/평창_대관령음악제.jpg","cr":"이하 이만식 - 네이버 블로그/Naver Blog / CC BY-SA 2.0 kr"},{"n":"강진 청자축제","c":"36390","m":[7,8],"t":"전통","d":"고려청자의 고장"},{"n":"부산 바다축제","c":"21090","m":[8],"t":"여름","d":"해운대 해변 축제","s":"20260807","e":"20260813","p":"festival_photos/부산_바다축제.jpg","cr":"85246.tistory / CC BY 4.0"},{"n":"무주 반딧불축제","c":"35330","m":[9],"t":"자연","d":"반딧불이 야행","s":"20260904","e":"20260912","p":"festival_photos/무주_반딧불축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"제천 국제음악영화제","c":"33030","m":[8],"t":"음악","d":"청풍호반 음악영화"},{"n":"안동 국제탈춤페스티벌","c":"37040","m":[9,10],"t":"전통","d":"하회탈과 세계 탈춤","s":"20260924","e":"20261004","p":"festival_photos/안동_국제탈춤페스티벌.JPG","cr":"hyolee2 / CC BY-SA 3.0"},{"n":"금산 인삼축제","c":"34310","m":[9,10],"t":"먹거리","d":"인삼 시장과 약초","p":"festival_photos/금산_인삼축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"산청 한방약초축제","c":"38370","m":[10],"t":"먹거리","d":"지리산 약초","s":"20261002","e":"20261011","p":"festival_photos/산청_한방약초축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"김제 지평선축제","c":"35060","m":[10],"t":"가을","d":"황금 들녘 지평선","s":"20261001","e":"20261005","p":"festival_photos/김제_지평선축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"안성 바우덕이축제","c":"31220","m":[9,10],"t":"전통","d":"남사당 줄타기","p":"festival_photos/안성_바우덕이축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"영동 난계국악축제","c":"33340","m":[10],"t":"음악","d":"국악의 고장","s":"20261015","e":"20261018","p":"festival_photos/영동_난계국악축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"진주 남강유등축제","c":"38030","m":[10],"t":"가을","d":"남강 위 유등 물결","s":"20261003","e":"20261018","p":"festival_photos/진주_남강유등축제.jpg","cr":"Asfreeas at Korean Wikipedia\n\n This photo was taken with Nikon D80 / CC BY 3.0"},{"n":"부산국제영화제","c":"21090","m":[10],"t":"영화","d":"아시아 최대 영화제"},{"n":"서울세계불꽃축제","c":"11190","m":[9],"t":"야경","d":"한강 여의도 불꽃","s":"20250927","e":"20250927","p":"festival_photos/서울세계불꽃축제.JPG","cr":"서울특별시 소방재난본부(Seoul Metropolitan Fire &amp; Disaster Headquarters) / CC BY-SA 4.0"},{"n":"이천 쌀문화축제","c":"31210","m":[10],"t":"먹거리","d":"임금님표 이천쌀"},{"n":"자라섬 재즈페스티벌","c":"31370","m":[10],"t":"음악","d":"섬에서 듣는 재즈"},{"n":"논산 강경젓갈축제","c":"34060","m":[10],"t":"먹거리","d":"강경 젓갈시장","s":"20251023","e":"20251026","p":"festival_photos/논산_강경젓갈축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"서산 해미읍성축제","c":"34050","m":[10],"t":"전통","d":"조선 읍성 체험","s":"20261009","e":"20261011","p":"festival_photos/서산_해미읍성축제.jpg","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"순천만 갈대축제","c":"36030","m":[10,11],"t":"가을","d":"순천만 갈대밭 노을"},{"n":"광주 세계김치축제","c":"24030","m":[10,11],"t":"먹거리","d":"김치 담그기와 시식","p":"festival_photos/광주_세계김치축제.JPG","cr":"한국관광공사 / 공공누리 (출처표시)"},{"n":"서울 빛초롱축제","c":"11010","m":[12,1],"t":"야경","d":"청계천 등불 거리","s":"20251212","e":"20260118","p":"festival_photos/서울_빛초롱축제.jpg","cr":"Seoul Lantern Festival / CC0"},{"n":"나라 와카쿠사야마 야키","c":"95029","m":[1],"t":"겨울","d":"산 전체를 태우는 불놓기"},{"n":"삿포로 눈축제","c":"91001","m":[2],"t":"겨울","d":"오도리공원 대형 눈조각","p":"festival_photos/삿포로_눈축제.JPG","cr":"Eckhard Pecher / CC BY 2.5"},{"n":"나가사키 랜턴 페스티벌","c":"98042","m":[2],"t":"야경","d":"붉은 등 1만 5천 개","p":"festival_photos/나가사키_랜턴_페스티벌.jpg","cr":"JKT-c / CC BY 3.0"},{"n":"아시카가 후지 등나무","c":"93009","m":[4,5],"t":"꽃","d":"밤에 빛나는 등나무 터널","p":"festival_photos/아시카가_후지_등나무.jpg","cr":"SLIMHANNYA / CC BY-SA 4.0"},{"n":"후지 시바자쿠라 축제","c":"94019","m":[4,5],"t":"꽃","d":"후지산 아래 분홍 꽃밭"},{"n":"삿포로 라일락축제","c":"91001","m":[5],"t":"꽃","d":"초여름 라일락","p":"festival_photos/삿포로_라일락축제.JPG","cr":"K. Takeda / CC BY-SA 3.0"},{"n":"나가라가와 우카이","c":"94021","m":[5,6,7,8,9],"t":"전통","d":"가마우지 야간 물고기잡이","p":"festival_photos/나가라가와_우카이.jpg","cr":"Ludger Heide / CC BY-SA 4.0"},{"n":"교토 기온마츠리","c":"95026","m":[7],"t":"전통","d":"일본 3대 마츠리, 야마보코 행렬","p":"festival_photos/교토_기온마츠리.jpg","cr":"江戸村のとくぞう / CC BY-SA 4.0"},{"n":"오사카 텐진마츠리","c":"95027","m":[7],"t":"전통","d":"배 행렬과 불꽃","p":"festival_photos/오사카_텐진마츠리.jpg","cr":"663highland / CC BY 2.5"},{"n":"하카타 기온 야마카사","c":"98040","m":[7],"t":"전통","d":"가마를 메고 달리는 새벽 질주","p":"festival_photos/하카타_기온_야마카사.jpg","cr":"枝折 / CC BY-SA 3.0"},{"n":"스미다가와 불꽃축제","c":"93013","m":[7],"t":"야경","d":"도쿄 여름 대표 불꽃","p":"festival_photos/스미다가와_불꽃축제.jpg","cr":"てんどん / CC BY 2.1 jp"},{"n":"아오모리 네부타 마츠리","c":"92002","m":[8],"t":"전통","d":"거대한 등 인형 행진"},{"n":"아키타 간토 마츠리","c":"92005","m":[8],"t":"전통","d":"장대에 매단 등불 묘기"},{"n":"센다이 다나바타 마츠리","c":"92004","m":[8],"t":"전통","d":"화려한 칠석 장식","p":"festival_photos/센다이_다나바타_마츠리.jpg","cr":"掬茶 / CC BY-SA 4.0"},{"n":"야마가타 하나가사 마츠리","c":"92006","m":[8],"t":"전통","d":"꽃갓 쓰고 추는 군무"},{"n":"도쿠시마 아와오도리","c":"97036","m":[8],"t":"전통","d":"400년 이어진 거리 춤","p":"festival_photos/도쿠시마_아와오도리.jpg","cr":"Rosino / CC BY-SA 2.0"},{"n":"나가오카 불꽃축제","c":"94015","m":[8],"t":"야경","d":"일본 3대 불꽃대회","p":"festival_photos/나가오카_불꽃축제.jpg","cr":"Koichi_Hayakawa / CC BY 4.0"},{"n":"오키나와 전도 에이사","c":"99047","m":[8,9],"t":"전통","d":"북을 치며 추는 에이사","p":"festival_photos/오키나와_전도_에이사.jpg","cr":"Kasanui]＆KI] / CC BY-SA 3.0"}];   // 매년 열리는 대표 축제 [{n:이름, c:코드, m:[월], t:태그, d:설명}]
const TRENDING = {"32030":{"s":92,"t":"바다 카페거리"},"39020":{"s":90,"t":"제주 남쪽 해안"},"36020":{"s":88,"t":"밤바다와 케이블카"},"21140":{"s":88,"t":"광안리 해변"},"11040":{"s":87,"t":"성수동 카페"},"39010":{"s":86,"t":"제주 북쪽 거점"},"37020":{"s":85,"t":"황리단길과 고분"},"21100":{"s":85,"t":"감천문화마을"},"35011":{"s":84,"t":"한옥마을"},"32410":{"s":84,"t":"서핑 해변"},"21090":{"s":83,"t":"해운대와 마린시티"},"36480":{"s":83,"t":"퍼플섬과 섬 여행"},"11140":{"s":82,"t":"연남·망원동"},"32060":{"s":82,"t":"설악과 항구"},"38090":{"s":81,"t":"바람의언덕과 해안"},"38050":{"s":80,"t":"한려수도 섬 여행"},"33380":{"s":80,"t":"도담삼봉과 패러글라이딩"},"11230":{"s":80,"t":"가로수길과 코엑스"},"11030":{"s":79,"t":"한남·이태원"},"11020":{"s":79,"t":"명동·DDP"},"31200":{"s":78,"t":"헤이리와 임진각"},"31370":{"s":78,"t":"남이섬과 계곡"},"32350":{"s":77,"t":"레일바이크와 하이원"},"38350":{"s":77,"t":"독일마을과 해안도로"},"11010":{"s":77,"t":"북촌·서촌 골목"},"35380":{"s":76,"t":"채석강과 변산"},"36460":{"s":76,"t":"청산도 슬로길"},"32340":{"s":76,"t":"대관령 목장과 스키"},"21010":{"s":75,"t":"자갈치·BIFF거리"},"36030":{"s":75,"t":"순천만 습지"},"31380":{"s":75,"t":"두물머리와 카페"},"37040":{"s":74,"t":"하회마을"},"32040":{"s":74,"t":"묵호 논골담길"},"26310":{"s":74,"t":"간절곶과 영남알프스"},"35370":{"s":73,"t":"선운사와 청보리밭"},"23010":{"s":73,"t":"개항장 거리"},"37060":{"s":73,"t":"부석사와 소수서원"},"36310":{"s":72,"t":"죽녹원 대숲"},"32070":{"s":72,"t":"장호항과 해상케이블카"},"34020":{"s":72,"t":"무령왕릉과 공산성"},"11050":{"s":72,"t":"뚝섬과 건대"},"35020":{"s":71,"t":"근대문화거리"},"34030":{"s":71,"t":"대천해수욕장"},"38400":{"s":71,"t":"해인사와 대장경"},"36010":{"s":70,"t":"근대역사거리와 항구"},"32010":{"s":70,"t":"호반과 닭갈비"},"34330":{"s":70,"t":"백제 유적과 궁남지"},"32020":{"s":70,"t":"소금산 출렁다리"},"22010":{"s":69,"t":"김광석거리·근대골목"},"33030":{"s":69,"t":"청풍호와 옥순봉"},"31270":{"s":69,"t":"아트밸리와 허브섬"},"24010":{"s":68,"t":"양림동 근대골목"},"32330":{"s":68,"t":"청령포와 별마로"},"37430":{"s":68,"t":"울릉도와 독도"},"26030":{"s":67,"t":"대왕암공원"},"34380":{"s":67,"t":"서해 해수욕장"},"37090":{"s":67,"t":"문경새재와 철로자전거"},"23310":{"s":67,"t":"고인돌과 해안 성곽"},"37011":{"s":66,"t":"호미곶과 스페이스워크"},"35040":{"s":66,"t":"내장산 단풍"},"31011":{"s":66,"t":"수원화성"},"32310":{"s":65,"t":"비발디파크와 은행나무숲"},"36360":{"s":65,"t":"녹차밭 다원"},"35050":{"s":65,"t":"광한루와 지리산 자락"},"25040":{"s":64,"t":"온천과 과학관"},"34050":{"s":64,"t":"해미읍성과 간월암"},"37360":{"s":63,"t":"프로방스와 소싸움"},"38360":{"s":63,"t":"화개장터와 차밭"},"38080":{"s":62,"t":"영남루와 얼음골"},"38070":{"s":62,"t":"가야 유적과 봉하마을"},"34040":{"s":62,"t":"온천과 외암마을"},"32360":{"s":61,"t":"한탄강 주상절리길"},"35030":{"s":60,"t":"미륵사지와 보석박물관"},"29010":{"s":60,"t":"호수공원과 수목원"},"93013":{"s":93,"t":"도심 전역"},"95027":{"s":90,"t":"먹거리와 야경"},"95026":{"s":89,"t":"고도의 사찰과 거리"},"91001":{"s":86,"t":"설경과 해산물"},"98040":{"s":83,"t":"포장마차와 근교"},"99047":{"s":82,"t":"바다와 리조트"},"98044":{"s":81,"t":"유후인·벳푸 온천"},"93014":{"s":78,"t":"요코하마·가마쿠라"},"93012":{"s":78,"t":"테마파크와 해안"},"94023":{"s":76,"t":"나고야 도심"},"95028":{"s":76,"t":"고베 야경과 히메지성"},"94019":{"s":75,"t":"후지산과 호수"},"95029":{"s":75,"t":"사슴공원과 고찰"},"96034":{"s":74,"t":"미야지마와 평화공원"},"94020":{"s":74,"t":"설산과 온천"},"94017":{"s":73,"t":"가나자와 옛거리"},"94022":{"s":72,"t":"후지 남쪽과 차밭"},"98043":{"s":71,"t":"아소산과 구마모토성"},"94021":{"s":71,"t":"시라카와고 합장마을"},"98046":{"s":70,"t":"사쿠라지마와 온천"},"98042":{"s":70,"t":"항구 야경과 하우스텐보스"},"94016":{"s":68,"t":"다테야마 알펜루트"},"97037":{"s":68,"t":"나오시마와 우동"},"92004":{"s":67,"t":"센다이와 마쓰시마"},"93009":{"s":67,"t":"닛코 사원과 단풍"},"95030":{"s":65,"t":"고야산과 구마노고도"},"97038":{"s":64,"t":"도고온천"},"96033":{"s":64,"t":"구라시키 미관지구"},"92003":{"s":62,"t":"히라이즈미와 산리쿠"},"94015":{"s":62,"t":"설국과 사도섬"}};     // 인기 여행지 {코드: {s:점수, t:테마}}
const TRENDING_AT = "2026-08";   // 인기도 기준일 (실시간 아님을 밝히기 위함)
const tooltip = document.getElementById("tooltip");
const $ = id => document.getElementById(id);

// ---- 나라별 지도 ----
const MAPS = {};
["kr", "jp"].forEach(cc => {
  const el = document.getElementById(cc === "kr" ? "map" : "mapJp");
  const vb0 = el.viewBox.baseVal;
  const home = { x: vb0.x, y: vb0.y, w: vb0.width, h: vb0.height };
  const ps = [...el.querySelectorAll(".g-munis path")];
  const marks = document.createElementNS("http://www.w3.org/2000/svg", "g");
  marks.setAttribute("pointer-events", "none");
  marks.setAttribute("class", "g-marks");
  el.insertBefore(marks, el.querySelector(".g-labels"));
  const byCode = {};
  ps.forEach(p => { byCode[p.dataset.code] = p; });
  const texts = [...el.querySelectorAll(".g-labels text")];
  // 라벨 수치를 미리 파싱해 둔다 (팬/줌마다 반복 파싱하지 않도록)
  const meta = texts.map(t => {
    const w = parseFloat(t.dataset.w) || 0;
    const h = parseFloat(t.dataset.h) || w;
    const name = t.textContent;
    // 군은 넓은 시골 지역인데도 도시의 작은 구들과 자리 다툼에서 밀리기 쉬워 우선순위를 준다
    const prio = name.endsWith("군") ? 1.6 : 1;
    return { t, code: t.dataset.code, w, h, area: w * h * prio,
             x: +t.getAttribute("x"), y: +t.getAttribute("y"),
             len: name.length };
  });
  // 큰 지역이 먼저 자리를 차지하도록 미리 정렬 (매 프레임 정렬 제거)
  const metaByArea = meta.slice().sort((a, b) => b.area - a.area);
  MAPS[cc] = {
    cc, svg: el, paths: ps,
    muniTexts: texts, labelMeta: meta, labelMetaByArea: metaByArea,
    provLabelsG: el.querySelector(".g-provlabels"),
    marksG: marks, pathByCode: byCode, bboxCache: {},
    home, vb: { ...home },
    labelAt: cc === "jp" ? Infinity : 6200,   // 이 폭(SVG 단위) 이하로 확대하면 지역명 표시
  };
});
let COUNTRY = "kr";
// 활성 지도 참조 (나라를 바꾸면 다시 바인딩)
let svg, paths, muniTexts, labelMeta, labelMetaByArea, provLabelsG, marksG, pathByCode, bboxCache, home, vb;
function bindMap(cc) {
  const m = MAPS[cc];
  COUNTRY = cc;
  svg = m.svg; paths = m.paths; muniTexts = m.muniTexts; provLabelsG = m.provLabelsG;
  labelMeta = m.labelMeta; labelMetaByArea = m.labelMetaByArea;
  marksG = m.marksG; pathByCode = m.pathByCode; bboxCache = m.bboxCache;
  home = m.home; vb = m.vb;
}
bindMap("kr");
// 시작은 한국 지도만 보이게
Object.values(MAPS).forEach(m => m.svg.classList.toggle("hidden", m.cc !== COUNTRY));
const MAPSVGS = Object.values(MAPS).map(m => m.svg);
// 지역명·소속은 두 나라를 합쳐서 참조 (검색·앨범용)
const nameByCode = {};
Object.values(MAPS).forEach(m => m.paths.forEach(p => { nameByCode[p.dataset.code] = p.dataset.name; }));
const countryOfCode = code => String(code)[0] === "9" ? "jp" : "kr";
// 통계 그룹: 도쿄 시·구(6자리)는 "9T", 그 외는 코드 앞 2자리
const groupOf = c => { c = String(c); return c.length === 6 ? "9T" : c.slice(0, 2); };

let ROOM = null, TOKEN = null;
let visited = new Set();
let photos = {};   // code -> [{url, name, vid}]
let notes = {};    // code -> {visits:[...]}
let selected = null;
let pollTimer = null;
let appliedStateFingerprint = "";

// 접속한 적 있는 지도 목록: {id: {name, token}}
const savedRooms = JSON.parse(localStorage.getItem("travelRooms") || "{}");
function rememberRoom(id, name, token) {
  savedRooms[id] = { name, token };
  localStorage.setItem("travelRooms", JSON.stringify(savedRooms));
}

// ---- API ----
// 쓰기 요청 추적 — 폴링이 방금 저장한 내용을 덮어쓰지 않게 하기 위함
let inflightWrites = 0, lastWriteAt = 0;
async function api(path, opts = {}) {
  const isWrite = !!opts.method && opts.method !== "GET";
  if (isWrite) inflightWrites++;
  opts.headers = Object.assign({ "x-token": TOKEN || "" }, opts.headers || {});
  try {
    const r = await fetch(path, opts);
    const body = await r.json().catch(() => ({}));
    if (!r.ok) throw Object.assign(new Error(body.error || r.status), { status: r.status });
    return body;
  } finally {
    if (isWrite) { inflightWrites--; lastWriteAt = Date.now(); }
  }
}
function setBadge(on, text) {
  const b = $("syncBadge");
  b.className = on ? "on" : "off";
  b.textContent = text || (on ? "동기화됨" : "연결 끊김");
}
const ICON_PIN = '<svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 21s-7-5.4-7-11a7 7 0 0 1 14 0c0 5.6-7 11-7 11Z"/><circle cx="12" cy="10" r="2.6"/></svg>';

// ---- 화면 전환 ----
function showLanding() {
  $("landing").style.display = "flex";
  $("app").style.display = "none";
  const ids = Object.keys(savedRooms);
  $("myRoomsWrap").style.display = ids.length ? "" : "none";
  $("myRooms").innerHTML = ids.map(id =>
    `<div class="room-row">` +
    `<a href="/?room=${encodeURIComponent(id)}">${ICON_PIN} ${escapeHtml(savedRooms[id].name || "")}</a>` +
    `<button class="room-forget" data-id="${escapeHtml(id)}" title="목록에서 지우기">&times;</button>` +
    `</div>`).join("");
  $("myRooms").querySelectorAll(".room-forget").forEach(b => {
    b.onclick = () => {
      // 목록에서만 빼는 것 — 지도 자체는 그대로 남는다 (링크로 다시 들어갈 수 있음)
      const id = b.dataset.id;
      if (!confirm(`"${savedRooms[id].name || id}"을(를) 목록에서 지울까요?\n지도는 삭제되지 않고, 링크로 다시 들어갈 수 있습니다.`)) return;
      delete savedRooms[id];
      localStorage.setItem("travelRooms", JSON.stringify(savedRooms));
      try { localStorage.removeItem("state:" + id); } catch {}
      showLanding();
    };
  });
}
// 마지막으로 받은 방 상태를 저장 — 오프라인에서도 지도·기록을 볼 수 있게
function cacheState(id, s) {
  try { localStorage.setItem("state:" + id, JSON.stringify(s)); } catch {}
}
function loadCachedState(id) {
  try { return JSON.parse(localStorage.getItem("state:" + id) || "null"); } catch { return null; }
}
function canonicalize(value) {
  if (Array.isArray(value)) return value.map(canonicalize);
  if (value && typeof value === "object") {
    return Object.keys(value).sort().reduce((out, key) => {
      out[key] = canonicalize(value[key]);
      return out;
    }, {});
  }
  return value;
}
function stateFingerprint(name, visitedValue, photosValue, notesValue) {
  return JSON.stringify(canonicalize({
    name: name || "",
    visited: [...visitedValue].map(String).sort(),
    photos: photosValue || {},
    notes: notesValue || {},
  }));
}
async function enterRoom(id) {
  ROOM = id;
  TOKEN = savedRooms[id] ? savedRooms[id].token : null;
  appliedStateFingerprint = "";
  resetThumbnailQueue();
  const cached = loadCachedState(id);
  let startedFromCache = false;
  if (cached) {
    startApp(cached, false);
    setBadge(false, "동기화 중…");
    startedFromCache = true;
  }
  try {
    const s = await api(`/api/state?room=${id}`);
    if (ROOM !== id) return;
    cacheState(id, s);
    if (startedFromCache) {
      $("roomName").textContent = s.name;
      rememberRoom(id, s.name, TOKEN);
      applyState(s);
      migrateMergedCodes().catch(() => {});
      setBadge(true);
      updateLabels();
      setTimeout(backfillThumbs, 3000);   // 첫 화면이 뜬 뒤에 조용히
    } else {
      startApp(s);
    }
  } catch (e) {
    if (e.status === 403 || e.status === 404) {
      if (startedFromCache) showLanding();
      showJoin(id);
      return;
    }
    // 네트워크 실패: 캐시된 상태가 있으면 오프라인 보기 모드로 진입
    if (startedFromCache) {
      setBadge(false, "오프라인 (보기 전용)");
      return;
    }
    if (cached) {
      startApp(cached);
      setBadge(false, "오프라인 (보기 전용)");
      return;
    }
    alert("서버에 연결할 수 없습니다.");
    showLanding();
  }
}
function showJoin(id) {
  $("joinModal").style.display = "flex";
  $("joinPw").focus();
  $("joinBtn").onclick = async () => {
    $("joinErr").textContent = "";
    try {
      const j = await api("/api/join", {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ room: id, password: $("joinPw").value })
      });
      TOKEN = j.token;
      rememberRoom(id, j.name, j.token);
      $("joinModal").style.display = "none";
      const s = await api(`/api/state?room=${id}`);
      cacheState(id, s);
      startApp(s);
    } catch (e) {
      $("joinErr").textContent = e.message === "Failed to fetch" ? "서버 연결 실패" : e.message;
    }
  };
  $("joinPw").onkeydown = e => { if (e.key === "Enter") $("joinBtn").click(); };
}
function startApp(state, runMigrations = true) {
  $("landing").style.display = "none";
  $("app").style.display = "flex";
  $("roomName").textContent = state.name;
  rememberRoom(ROOM, state.name, TOKEN);
  applyState(state);
  // 통합된 옛 코드의 방문·메모·사진을 새 코드로 이관 (있을 때만).
  // 비동기라 실패해도 앱 시작은 계속되게 둔다.
  if (runMigrations) migrateMergedCodes().catch(() => {});
  setBadge(true);
  updateLabels();
  setTimeout(backfillThumbs, 3000);   // 첫 화면이 뜬 뒤에 조용히
  maybeShowFirstHint();
  clearInterval(pollTimer);
  pollTimer = setInterval(refresh, 12000);   // 다른 사람의 변경 반영
}
// 지도에서 합쳐진 지역의 옛 코드를 새 코드로 이관한다.
//  - 도쿄 시·구(913xxx) → 도쿄도(93013)
//  - 한국 일반구(용인시수지구 등) → 모도시 (전주·청주·창원·용인 등 12개시)
const MERGED_CODES = {"38115": "38111", "38114": "38111", "38113": "38111", "38112": "38111", "37012": "37011", "35012": "35011", "34012": "34011", "33012": "33011", "31193": "31191", "31192": "31191", "31104": "31101", "31103": "31101", "31092": "31091", "31053": "31051", "31052": "31051", "31042": "31041", "31023": "31021", "31022": "31021", "31014": "31011", "31013": "31011", "31012": "31011"};
function mergedTargetOf(code) {
  if (/^913\d{3}$/.test(code)) return "93013";
  return MERGED_CODES[code] || null;
}
// 통합으로 사라진 옛 코드(도쿄 시·구, 일반구)의 기록을 새 코드로 옮긴다
async function migrateMergedCodes() {
  const pairs = [];
  [...visited, ...Object.keys(notes), ...Object.keys(photos)].forEach(c => {
    const t = mergedTargetOf(String(c));
    if (t && !pairs.some(p => p[0] === String(c))) pairs.push([String(c), t]);
  });
  if (!pairs.length) return;
  for (const [from, to] of pairs) await migrateCode(from, to);
}
async function migrateCode(from, to) {
  const oldVisits = visited.has(from) ? [from] : [];
  const oldNotes = notes[from] ? [from] : [];
  const oldPhotos = photos[from] ? [...photos[from]] : [];
  if (!oldVisits.length && !oldNotes.length && !oldPhotos.length) return;
  markDirty(to);
  markDirty(from);
  // 방문 이관
  if (oldVisits.length) {
    if (!visited.has(to)) {
      visited.add(to);
      api("/api/visited", { method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ room: ROOM, code: to, on: true }) }).catch(() => {});
    }
    oldVisits.forEach(c => {
      visited.delete(c); markDirty(c);
      api("/api/visited", { method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ room: ROOM, code: c, on: false }) }).catch(() => {});
    });
  }
  // 메모(방문 기록) 이관
  if (oldNotes.length) {
    const merged = getVisits(to);
    oldNotes.forEach(c => {
      getVisits(c).forEach(v => merged.push({ id: v.id || newVid(), start: v.start, end: v.end, memo: v.memo }));
      delete notes[c]; markDirty(c);
      api("/api/note", { method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ room: ROOM, code: c, visits: [] }) }).catch(() => {});
    });
    notes[to] = { visits: merged };
    api("/api/note", { method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ room: ROOM, code: to, visits: merged }) }).catch(() => {});
  }
  // 사진도 이전 지역 코드에서 새 모도시 코드로 이동
  if (oldPhotos.length) {
    photos[to] = photos[to] || [];
    for (const p of oldPhotos) {
      if (!photos[to].some(x => x.url === p.url)) photos[to].push(p);
      try {
        await api(`/api/photo?room=${ROOM}&code=${from}&to=${to}`, {
          method: "PUT", headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ url: p.url })
        });
        photos[from] = (photos[from] || []).filter(x => x.url !== p.url);
        if (!photos[from].length) delete photos[from];
      } catch {}
    }
  }
  paintVisited(); render(); renderFeed();
}
function paintVisited() {
  // 두 나라 지도 모두 방문 색칠을 갱신 (나라를 바꿔도 색이 유지되도록)
  Object.values(MAPS).forEach(m =>
    m.paths.forEach(p => p.classList.toggle("visited", visited.has(p.dataset.code))));
}
// 최근에 로컬 편집한 지역 (code -> 만료 시각). 폴링이 이 지역을 덮어쓰지 않게 함.
const dirty = new Map();
function markDirty(code) { dirty.set(String(code), Date.now() + 6000); }
function applyState(s) {
  const now = Date.now();
  const inVisited = new Set((s.visited || []).map(String));
  const inPhotos = s.photos || {};
  const inNotes = s.notes || {};
  // 최근 로컬 편집(dirty) 지역은 서버 상태 대신 로컬 값을 유지 (동시 편집/폴링 클로버 방지)
  dirty.forEach((exp, code) => {
    if (exp < now) { dirty.delete(code); return; }
    if (visited.has(code)) inVisited.add(code); else inVisited.delete(code);
    if (notes[code]) inNotes[code] = notes[code]; else delete inNotes[code];
    if (photos[code]) inPhotos[code] = photos[code]; else delete inPhotos[code];
  });
  const fingerprint = stateFingerprint(s.name, inVisited, inPhotos, inNotes);
  const changed = fingerprint !== appliedStateFingerprint;
  visited = inVisited;
  photos = inPhotos;
  notes = inNotes;
  appliedStateFingerprint = fingerprint;
  if (!changed) return false;
  paintVisited();
  render();
  renderFeed();
  if (selected) renderPanel();
  return true;
}
async function refresh() {
  // 저장 중이거나 방금 저장했으면 건너뜀 (서버 응답이 뒤늦게 와서 로컬 편집을 지우는 것 방지)
  if (inflightWrites > 0 || Date.now() - lastWriteAt < 4000) return;
  // 입력 중이면 건너뜀 (타이핑하던 메모가 사라지지 않게)
  const ae = document.activeElement;
  if (ae && regionPanelEl && regionPanelEl.contains(ae)) return;
  try {
    const s = await api(`/api/state?room=${ROOM}`);
    cacheState(ROOM, s);
    const changed = applyState(s);
    setBadge(true);
    if (changed) updateLabels();
  } catch { setBadge(false, navigator.onLine ? "연결 끊김" : "오프라인 (보기 전용)"); }
}

$("createBtn").onclick = async () => {
  $("createErr").textContent = "";
  try {
    const j = await api("/api/rooms", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ name: $("newName").value.trim(), password: $("newPw").value })
    });
    TOKEN = j.token;
    rememberRoom(j.id, j.name, j.token);
    location.href = "/?room=" + j.id;
  } catch (e) {
    $("createErr").textContent = e.message === "Failed to fetch" ? "서버 연결 실패" : e.message;
  }
};
const inviteBtnHtml = $("inviteBtn").innerHTML;
$("inviteBtn").onclick = async () => {
  const link = location.origin + "/?room=" + ROOM;
  try { await navigator.clipboard.writeText(link); $("inviteBtn").textContent = "✓ 복사됨!"; }
  catch { prompt("초대 링크:", link); }
  setTimeout(() => { $("inviteBtn").innerHTML = inviteBtnHtml; }, 1500);
};
$("leaveBtn").onclick = () => { location.href = "/"; };

// ---- 지도 이미지 저장 / 공유 ----
const emblemDataCache = {};
async function toDataURL(url) {
  if (emblemDataCache[url]) return emblemDataCache[url];
  const blob = await (await fetch(url)).blob();
  const data = await new Promise(res => {
    const fr = new FileReader();
    fr.onload = () => res(fr.result);
    fr.readAsDataURL(blob);
  });
  emblemDataCache[url] = data;
  return data;
}

async function buildMapImage() {
  const W = 800, H = 1100, HEADER = 132, DPR = 2;
  try { await document.fonts.ready; } catch {}   // 폰트 로드 후 렌더 (헤더 텍스트용)
  // 1) 지도 SVG 복제 (전국 뷰) + 방문 색 인라인 + 심벌 data URI
  const clone = svg.cloneNode(true);
  // 전국(홈) 뷰로 되돌린다. 좌표계가 SVG 단위이므로 home 값을 그대로 쓴다
  clone.setAttribute("viewBox", `${home.x} ${home.y} ${home.w} ${home.h}`);
  clone.removeAttribute("id");
  clone.querySelectorAll(".g-munis path").forEach(p => {
    p.classList.remove("visited", "selected");
    p.setAttribute("fill", visited.has(p.dataset.code) ? "#b7e4c7" : "#ffffff");
    p.setAttribute("stroke", "#c6cfd9");
    p.setAttribute("stroke-width", "5.5");   // 좌표계가 10배이므로 선 두께도 10배
  });
  // 라벨은 전국 뷰에서 겹치므로 제거, 마커만 다시 그림
  const cLabels = clone.querySelector(".g-labels"); if (cLabels) cLabels.remove();
  const cMarks = clone.querySelector(".g-marks"); if (cMarks) cMarks.remove();
  // 방문 심벌을 지역 라벨 중심(가장 큰 섬의 무게중심)에 data URI로 배치
  // — bbox 중심을 쓰면 먼 섬(이즈 제도 등)이 포함된 지역은 마커가 바다로 밀림
  // 겹침 회피로 밀어낸 y가 아니라 원래 라벨 좌표를 쓴다 (내보내기는 전국 뷰라 이동값이 무의미)
  const labelPos = {};
  labelMeta.forEach(m => { labelPos[m.code] = [m.x, m.y]; });
  const markG = document.createElementNS("http://www.w3.org/2000/svg", "g");
  for (const p of paths) {
    if (!visited.has(p.dataset.code)) continue;
    const bb = p.getBBox();
    const em = EMBLEMS[p.dataset.code];
    const size = Math.min(Math.min(bb.width, bb.height) * 0.9, 260);   // 상한도 SVG 단위(10배)
    const pos = labelPos[p.dataset.code] || [bb.x + bb.width / 2, bb.y + bb.height / 2];
    const cx = pos[0], cy = pos[1];
    if (em) {
      const data = await toDataURL(em);
      const img = document.createElementNS("http://www.w3.org/2000/svg", "image");
      img.setAttributeNS("http://www.w3.org/1999/xlink", "href", data);
      img.setAttribute("href", data);
      img.setAttribute("x", cx - size / 2); img.setAttribute("y", cy - size / 2);
      img.setAttribute("width", size); img.setAttribute("height", size);
      img.setAttribute("preserveAspectRatio", "xMidYMid meet");
      markG.appendChild(img);
    }
  }
  clone.appendChild(markG);
  clone.setAttribute("xmlns", "http://www.w3.org/2000/svg");
  const svgStr = new XMLSerializer().serializeToString(clone);
  const svgImg = new Image();
  await new Promise((res, rej) => {
    svgImg.onload = res; svgImg.onerror = rej;
    svgImg.src = "data:image/svg+xml;charset=utf-8," + encodeURIComponent(svgStr);
  });
  // 2) 캔버스에 헤더 + 지도
  const canvas = document.createElement("canvas");
  canvas.width = W * DPR; canvas.height = (H + HEADER) * DPR;
  const ctx = canvas.getContext("2d");
  ctx.scale(DPR, DPR);
  ctx.fillStyle = "#ffffff"; ctx.fillRect(0, 0, W, H + HEADER);
  // 헤더 텍스트 (현재 보고 있는 나라 기준, 지도에 존재하는 코드만)
  let n = 0;
  visited.forEach(c => { if (countryOfCode(c) === COUNTRY && nameByCode[c]) n++; });
  const unit = COUNTRY === "jp" ? "도도부현" : "시·군·구";
  const pct = Math.round(n / paths.length * 1000) / 10;
  ctx.fillStyle = "#1f2937";
  ctx.font = "700 34px 'Pretendard Variable',Pretendard,'Malgun Gothic',sans-serif";
  ctx.textBaseline = "alphabetic";
  ctx.fillText($("roomName").textContent || "나의 여행 지도", 40, 58);
  ctx.fillStyle = "#3b9c6e";
  ctx.font = "700 26px 'Pretendard Variable',Pretendard,'Malgun Gothic',sans-serif";
  const pctStr = `${pct}%`;
  ctx.fillText(pctStr, 40, 100);
  const pctW = ctx.measureText(pctStr).width;
  ctx.fillStyle = "#6b7280";
  ctx.font = "400 18px 'Pretendard Variable',Pretendard,'Malgun Gothic',sans-serif";
  ctx.fillText(`${n} / ${paths.length} ${unit} 방문`, 40 + pctW + 12, 100);
  ctx.textAlign = "right";
  ctx.fillStyle = "#9ca3af";
  ctx.font = "400 15px 'Pretendard Variable',Pretendard,'Malgun Gothic',sans-serif";
  ctx.fillText("map-for-memory", W - 40, 100);
  ctx.textAlign = "left";
  ctx.strokeStyle = "#eef0f3"; ctx.lineWidth = 1;
  ctx.beginPath(); ctx.moveTo(40, HEADER - 12); ctx.lineTo(W - 40, HEADER - 12); ctx.stroke();
  ctx.drawImage(svgImg, 0, HEADER, W, H);
  return await new Promise(res => canvas.toBlob(res, "image/png"));
}

$("saveBtn").onclick = async () => {
  const btn = $("saveBtn");
  const orig = btn.innerHTML;
  btn.textContent = "만드는 중…"; btn.disabled = true;
  try {
    const blob = await buildMapImage();
    const fname = `${($("roomName").textContent || "여행지도").replace(/[\\/:*?"<>|]/g, "")}.png`;
    const file = new File([blob], fname, { type: "image/png" });
    if (navigator.canShare && navigator.canShare({ files: [file] })) {
      await navigator.share({ files: [file], title: $("roomName").textContent });
    } else {
      const a = document.createElement("a");
      a.href = URL.createObjectURL(blob);
      a.download = fname;
      a.click();
      setTimeout(() => URL.revokeObjectURL(a.href), 1000);
    }
  } catch (e) {
    if (e && e.name !== "AbortError") alert("이미지를 만들지 못했어요. 다시 시도해 주세요.");
  } finally {
    btn.innerHTML = orig; btn.disabled = false;
  }
};

// ---- 탭 = 지역 선택만 (방문 표시는 패널의 버튼으로) ----
// 탭 위치의 지역을 찾되, 정확히 못 맞히면 화면상 가장 가까운 지역으로 스냅 (작은 구 대응)
function regionAt(clientX, clientY) {
  const el = document.elementFromPoint(clientX, clientY);
  if (el && el.parentElement && el.parentElement.classList.contains("g-munis") && el.dataset.code)
    return el.dataset.code;
  const r = svg.getBoundingClientRect();
  if (!r.width) return null;
  const px = vb.x + (clientX - r.left) / r.width * vb.w;
  const py = vb.y + (clientY - r.top) / r.height * vb.h;
  let best = null, bestD = Infinity;
  for (const p of paths) {
    const bb = regionBBox(p.dataset.code);
    if (!bb || !bb.width) continue;
    const cx = bb.x + bb.width / 2, cy = bb.y + bb.height / 2;
    const d = Math.hypot(cx - px, cy - py);
    if (d < bestD) { bestD = d; best = p; }
  }
  const tolSvg = 26 / (r.width / vb.w);   // 화면상 26px 이내면 스냅
  return best && bestD < tolSvg ? best.dataset.code : null;
}
function handleTap(clientX, clientY) {
  const code = regionAt(clientX, clientY);
  if (code) select(code);
}
function toggleVisited(code) {
  markDirty(code);
  const p = paths.find(x => x.dataset.code === code);
  const on = !visited.has(code);
  if (on) { visited.add(code); p.classList.add("visited"); }
  else { visited.delete(code); p.classList.remove("visited"); }
  render(); updateLabels();
  api("/api/visited", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ room: ROOM, code, on })
  }).then(() => setBadge(true)).catch(() => setBadge(false));
}

// ---- 모바일 레이아웃: 하단 탭 + 바텀시트 ----
const mq = window.matchMedia("(max-width: 720px)");
const asideEl = document.querySelector("aside");
const regionPanelEl = $("regionPanel");
function layoutMode() {
  document.body.classList.toggle("is-mobile", mq.matches);
  if (mq.matches) {
    document.body.appendChild(regionPanelEl);   // 바텀시트로 이동
  } else {
    // 사이드바 상단(추천·앨범 위)으로 복귀.
    // 기준점은 aside의 '직계' 자식이어야 한다 — 섹션 안쪽 요소를 쓰면 insertBefore가 터진다.
    asideEl.insertBefore(regionPanelEl, $("recSection"));
    regionPanelEl.classList.remove("open");
    setTab("map");
  }
}
function setTab(t) {
  // 추천과 기록은 같은 화면(사이드) 안에 있고, 어디로 스크롤할지만 다르다
  document.body.classList.toggle("tab-stats", t !== "map");
  document.body.classList.toggle("tab-rec", t === "rec");
  $("tabMap").classList.toggle("active", t === "map");
  $("tabStats").classList.toggle("active", t === "stats");
  $("tabRec").classList.toggle("active", t === "rec");
  if (t === "map") { updateLabels(); return; }   // 숨겨져 있던 동안 크기가 0이었으므로 다시 계산
  // 모바일은 추천/기록이 서로 다른 화면이라 위에서부터 보여주면 된다.
  // 데스크톱은 한 화면에 다 있으므로 해당 섹션으로 스크롤한다.
  const head = t === "rec" ? $("recHead") : $("feedHead");
  asideEl.scrollTo({ top: mq.matches ? 0 : Math.max(0, head.offsetTop - 8), behavior: "smooth" });
}
$("tabMap").onclick = () => setTab("map");
$("tabStats").onclick = () => setTab("stats");
$("tabRec").onclick = () => setTab("rec");
$("mapStatPill").onclick = () => setTab("stats");
$("rpClose").onclick = () => {
  regionPanelEl.classList.remove("open");
  paths.forEach(p => p.classList.remove("selected"));
  selected = null;
};
mq.addEventListener("change", layoutMode);
window.addEventListener("resize", layoutMode);
layoutMode();

// ---- 선택 지역 패널 ----
function select(code) {
  setCountry(countryOfCode(code));   // 다른 나라 지역이면 지도부터 전환
  selected = code;
  dismissFirstHint();
  paths.forEach(p => p.classList.toggle("selected", p.dataset.code === code));
  renderPanel();
  if (mq.matches) regionPanelEl.classList.add("open");
}
function renderPanel() {
  if (!selected) return;
  const p = paths.find(x => x.dataset.code === selected);
  $("rpHint").style.display = "none";
  $("rpBody").style.display = "";
  $("rpName").textContent = p.dataset.name;
  $("rpProv").textContent = PROV[groupOf(selected)] || "";
  const emblem = EMBLEMS[selected];
  $("rpEmblem").style.display = emblem ? "block" : "none";
  if (emblem) $("rpEmblem").src = emblem;
  const v = visited.has(selected);
  const t = $("rpToggle");
  t.textContent = v ? "✓ 방문함" : "방문 표시";
  t.className = v ? "v" : "";
  renderVisits();
  // 방문에 속하지 않은 사진(예전 기록)만 따로 표시
  const unassigned = unassignedPhotos(selected);
  $("unassignedHead").style.display = unassigned.length ? "" : "none";
  const grid = $("photoGrid");
  grid.innerHTML = unassigned.length ? photoGridHtml(unassigned) : "";
  wirePhotoGrid(grid, selected);
}
$("rpToggle").onclick = () => { if (selected) { toggleVisited(selected); renderPanel(); } };

// ---- 방문 기록(여러 번 방문 지원) ----
// 저장 형태: notes[code] = { visits: [{start, end, memo}] }
// 예전 형태({date, memo})는 읽을 때 자동 변환
function newVid() { return "v" + Date.now().toString(36) + Math.random().toString(36).slice(2, 6); }

// ---- 사진을 다른 방문으로 옮기기 ----
function openMovePicker(code, url) {
  const cur = ((photos[code] || []).find(p => p.url === url) || {}).vid || "";
  const vs = getVisits(code);
  const rows = vs.map(v => {
    const d = visitDateText(v) || "날짜 없는 방문";
    const memo = v.memo ? `<span class="mv-memo">${escapeHtml(v.memo.slice(0, 30))}</span>` : "";
    return `<button data-vid="${v.id}" class="${v.id === cur ? "cur" : ""}">${d}${memo}</button>`;
  });
  rows.push(`<button data-vid="" class="${cur ? "" : "cur"}">방문일 미지정</button>`);
  if (!vs.length) {
    rows.unshift(`<div class="mv-memo" style="margin-bottom:8px">먼저 '+ 방문 추가'로 방문 기록을 만들어 주세요.</div>`);
  }
  $("moveList").innerHTML = rows.join("");
  $("moveList").querySelectorAll("button[data-vid]").forEach(b => {
    b.onclick = () => movePhoto(code, url, b.dataset.vid);
  });
  $("movePicker").classList.add("show");
}
$("moveCancel").onclick = () => $("movePicker").classList.remove("show");
$("movePicker").onclick = (e) => { if (e.target.id === "movePicker") $("movePicker").classList.remove("show"); };
async function movePhoto(code, url, vid) {
  markDirty(code);
  $("movePicker").classList.remove("show");
  try {
    await api(`/api/photo?room=${ROOM}&code=${code}&vid=${encodeURIComponent(vid)}`, {
      method: "PUT", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ url })
    });
    const p = (photos[code] || []).find(x => x.url === url);
    if (p) p.vid = vid;
    renderPanel(); renderFeed();
    setBadge(true);
  } catch { alert("옮기지 못했어요. 다시 시도해 주세요."); setBadge(false); }
}
function normVisits(raw) {
  if (!raw) return [];
  if (Array.isArray(raw.visits)) return raw.visits.map(v => ({
    id: v.id || newVid(), start: v.start || "", end: v.end || "", memo: v.memo || ""
  }));
  if (raw.date || raw.memo) return [{ id: newVid(), start: raw.date || "", end: "", memo: raw.memo || "" }];
  return [];
}
function getVisits(code) {
  const raw = notes[code];
  const vs = normVisits(raw);
  // 예전 데이터엔 방문 ID가 없어 매번 새로 생기므로, 한 번 부여하고 고정한다
  // (고정하지 않으면 사진-방문 연결이 계속 어긋남)
  const missing = vs.length && (!raw || !Array.isArray(raw.visits) || raw.visits.some(v => !v.id));
  if (missing) {
    notes[code] = { visits: vs };
    if (ROOM) {
      api("/api/note", {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ room: ROOM, code, visits: vs })
      }).catch(() => {});
    }
  }
  return vs;
}
// 방문별 사진 / 방문일 미지정 사진
function photosOfVisit(code, vid) {
  return (photos[code] || []).filter(p => (p.vid || "") === vid);
}
function unassignedPhotos(code) {
  const ids = new Set(getVisits(code).map(v => v.id));
  return (photos[code] || []).filter(p => !p.vid || !ids.has(p.vid));
}
function photoGridHtml(list) {
  if (!list.length) return "";
  return `<div class="v-photos">${list.map(p =>
    `<div class="ph"><img src="${thumbOf(p)}" alt="${escapeHtml(p.name || "")}" loading="lazy" decoding="async" data-full="${p.url}">` +
    `<button class="del" data-url="${p.url}" title="삭제">✕</button>` +
    `<button class="mv" data-url="${p.url}" title="다른 방문으로 옮기기">이동</button></div>`).join("")}</div>`;
}
// 그리드 안의 사진 클릭(확대)·삭제 연결
function wirePhotoGrid(root, code) {
  const urls = [...root.querySelectorAll("img[data-full]")].map(i => i.dataset.full);
  root.querySelectorAll("img[data-full]").forEach(img => {
    img.onclick = (ev) => {
      ev.stopPropagation();
      openLightbox(urls, img.dataset.full);
    };
  });
  root.querySelectorAll("button.mv[data-url]").forEach(btn => {
    btn.onclick = (ev) => { ev.stopPropagation(); openMovePicker(code, btn.dataset.url); };
  });
  root.querySelectorAll("button.del[data-url]").forEach(btn => {
    btn.onclick = async (ev) => {
      ev.stopPropagation();
      if (!confirm("이 사진을 삭제할까요?")) return;
      const url = btn.dataset.url;
      markDirty(code);
      try {
        await api(`/api/photo?room=${ROOM}&code=${code}`, {
          method: "DELETE", headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ url })
        });
        photos[code] = (photos[code] || []).filter(x => x.url !== url);
        if (!photos[code].length) delete photos[code];
        renderPanel(); renderFeed();
      } catch { alert("삭제 실패"); }
    };
  });
}

function renderVisits() {
  const list = $("visitList");
  const visits = getVisits(selected);
  list.innerHTML = visits.map((v, i) => `
    <div class="visit-card" data-i="${i}">
      <div class="visit-top">
        <span class="dlabel">📅 방문일 <em>(하루면 시작일만)</em></span>
        <button class="visit-del" title="이 방문 기록 삭제">✕</button>
      </div>
      <div class="visit-dates">
        <input type="date" class="v-start" value="${v.start}" max="2999-12-31" aria-label="시작일">
        <span class="tilde">~</span>
        <input type="date" class="v-end" value="${v.end}" max="2999-12-31" aria-label="종료일(선택)">
      </div>
      <textarea rows="2" maxlength="500" class="v-memo" placeholder="이때의 기억을 남겨보세요">${escapeHtml(v.memo)}</textarea>
      ${photoGridHtml(photosOfVisit(selected, v.id))}
      <button class="v-addphoto" data-vid="${v.id}">📷 이 방문의 사진 추가</button>
    </div>`).join("");
  list.querySelectorAll(".visit-card").forEach(card => {
    const i = +card.dataset.i;
    const start = card.querySelector(".v-start");
    const end = card.querySelector(".v-end");
    const memo = card.querySelector(".v-memo");
    const commit = () => {
      const vs = getVisits(selected);
      if (!vs[i]) return;
      // 종료일이 시작일보다 빠르면 자동 교정
      let s = start.value, e = end.value;
      if (s && e && e < s) { e = s; end.value = e; }
      vs[i] = { id: vs[i].id, start: s, end: e, memo: memo.value };   // id 유지 (사진 연결)
      saveVisits(selected, vs);
    };
    start.onchange = commit;
    end.onchange = commit;
    let tmr = null;
    memo.oninput = () => { clearTimeout(tmr); tmr = setTimeout(commit, 600); };
    memo.onblur = () => { clearTimeout(tmr); commit(); };
    card.querySelector(".visit-del").onclick = async () => {
      const code = selected;
      const vs = getVisits(code);
      const gone = vs[i];
      const ph = gone ? photosOfVisit(code, gone.id) : [];
      if (ph.length && !confirm(`이 방문 기록과 사진 ${ph.length}장이 함께 삭제됩니다. 계속할까요?`)) return;
      for (const p of ph) {
        try {
          await api(`/api/photo?room=${ROOM}&code=${code}`, {
            method: "DELETE", headers: { "Content-Type": "application/json" },
            body: JSON.stringify({ url: p.url })
          });
        } catch {}
        photos[code] = (photos[code] || []).filter(x => x.url !== p.url);
      }
      if (photos[code] && !photos[code].length) delete photos[code];
      if (gone) noteRemoved(code, gone.id);   // 서버가 이 항목만 지우도록
      vs.splice(i, 1);
      saveVisits(code, vs);
      renderPanel();
    };
    card.querySelector(".v-addphoto").onclick = () => {
      pendingVid = card.querySelector(".v-addphoto").dataset.vid;
      $("photoInput").click();
    };
    wirePhotoGrid(card, selected);
  });
}
// 지운 방문 기록의 id를 지역별로 모아둔다.
// 서버는 '안 보낸 항목'이 삭제인지 모르는 것인지 구분할 수 없으므로,
// 지운 id를 함께 보내야 같이 쓰는 사람의 기록을 지우지 않고 합칠 수 있다.
const removedVisits = new Map();
function noteRemoved(code, id) {
  if (!id) return;
  const set = removedVisits.get(code) || new Set();
  set.add(id);
  removedVisits.set(code, set);
}
function saveVisits(code, visits) {
  markDirty(code);
  // 사용자가 명시적으로 추가한 방문은 비어 있어도 유지 (사진·날짜를 나중에 채울 수 있게)
  const clean = visits.filter(v => v.id);
  if (clean.length) notes[code] = { visits: clean };
  else delete notes[code];
  // 방문 기록을 남겼는데 아직 미방문이면 자동으로 방문 처리
  if (clean.length && !visited.has(code)) toggleVisited(code);
  renderFeed();
  const removed = [...(removedVisits.get(code) || [])];
  api("/api/note", {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ room: ROOM, code, visits: clean, removed })
  }).then(r => {
    setBadge(true);
    // 서버가 합쳐준 결과를 받아 다른 사람이 같은 지역에 남긴 기록도 바로 보이게 한다
    if (r && Array.isArray(r.visits)) {
      if (r.visits.length) notes[code] = { visits: r.visits };
      else delete notes[code];
      renderFeed();
      if (selected === code) renderVisits();
    }
  }).catch(() => setBadge(false));
}
let pendingVid = "";   // 어느 방문 기록에 사진을 올릴지
$("addVisitBtn").onclick = () => {
  if (!selected) return;
  const vs = getVisits(selected);
  vs.push({ id: newVid(), start: "", end: "", memo: "" });
  saveVisits(selected, vs);   // 바로 저장해야 동기화에 지워지지 않음
  renderVisits();
  const last = $("visitList").querySelector(".visit-card:last-child .v-start");
  if (last) last.focus();
};
// 날짜(기간) 표시용 문자열
function visitDateText(v) {
  const f = d => d.replace(/-/g, ".");
  if (v.start && v.end && v.end !== v.start) {
    // 같은 해·달이면 끝 날짜는 일만 (2026.07.15~16)
    const [ys, ms] = v.start.split("-"), [ye, me, de] = v.end.split("-");
    if (ys === ye && ms === me) return `${f(v.start)}~${de}`;
    if (ys === ye) return `${f(v.start)}~${me}.${de}`;
    return `${f(v.start)}~${f(v.end)}`;
  }
  return f(v.start || v.end || "");
}

// 여행 앨범 피드 (기록 탭): 방문 날짜 최신순, 사진·메모 있는 곳 위주
// ---- 여행 통계 카드 (현재 보고 있는 나라 기준) ----
function renderStatsCards() {
  const box = $("statsCards");
  if (!box) return;
  const cards = [];
  const thisYear = new Date().getFullYear();
  let photoCount = 0, visitCount = 0, yearCount = 0, longest = null, firstD = null, lastD = null;
  const regionVisits = [];   // {code, n} 방문 횟수
  const yearSet = new Set();

  Object.keys(nameByCode).forEach(code => {
    if (countryOfCode(code) !== COUNTRY) return;
    const vs = getVisits(code).filter(v => v.start || v.end || v.memo);
    const ph = (photos[code] || []).length;
    photoCount += ph;
    if (vs.length) regionVisits.push({ code, n: vs.length });
    vs.forEach(v => {
      visitCount++;
      const s = v.start || v.end;
      if (s) {
        const y = +s.slice(0, 4);
        yearSet.add(y);
        if (y === thisYear) yearCount++;
        if (!firstD || s < firstD) firstD = s;
        if (!lastD || s > lastD) lastD = s;
        // 가장 긴 여행(기간)
        if (v.start && v.end && v.end > v.start) {
          const days = Math.round((Date.parse(v.end) - Date.parse(v.start)) / 86400000) + 1;
          if (!longest || days > longest.days) longest = { code, days, text: visitDateText(v) };
        }
      }
    });
  });

  const visitedHere = [...visited].filter(c => countryOfCode(c) === COUNTRY && nameByCode[c]).length;
  if (!visitedHere && !visitCount && !photoCount) {
    box.innerHTML = "";
    $("statsCardHead").style.display = "none";
    return;
  }

  const unit = COUNTRY === "jp" ? "도도부현" : "시·군·구";
  cards.push(`<div class="stat-card"><div class="sc-label">방문한 곳</div>
    <div class="sc-value"><em>${visitedHere}</em>곳</div>
    <div class="sc-sub">전체 ${paths.length}${unit}</div></div>`);

  if (yearCount) {
    cards.push(`<div class="stat-card"><div class="sc-label">올해 여행</div>
      <div class="sc-value"><em>${yearCount}</em>번</div>
      <div class="sc-sub">${thisYear}년 기록</div></div>`);
  } else if (visitCount) {
    cards.push(`<div class="stat-card"><div class="sc-label">남긴 여행 기록</div>
      <div class="sc-value"><em>${visitCount}</em>번</div>
      <div class="sc-sub">${yearSet.size ? yearSet.size + "개 연도" : "날짜 미기록"}</div></div>`);
  }

  const most = regionVisits.sort((a, b) => b.n - a.n)[0];
  if (most && most.n > 1) {
    cards.push(`<div class="stat-card"><div class="sc-label">가장 많이 간 곳</div>
      <div class="sc-value">${escapeHtml(nameByCode[most.code])}</div>
      <div class="sc-sub">${most.n}번 방문</div></div>`);
  }
  if (photoCount) {
    cards.push(`<div class="stat-card"><div class="sc-label">사진</div>
      <div class="sc-value"><em>${photoCount}</em>장</div>
      <div class="sc-sub">추억 기록</div></div>`);
  }
  if (longest) {
    cards.push(`<div class="stat-card"><div class="sc-label">가장 긴 여행</div>
      <div class="sc-value">${escapeHtml(nameByCode[longest.code] || "")} <em>${longest.days}일</em></div>
      <div class="sc-sub">${longest.text}</div></div>`);
  }
  if (firstD && lastD && firstD !== lastD) {
    const f = d => d.replace(/-/g, ".");
    cards.push(`<div class="stat-card wide"><div class="sc-label">여행의 기록</div>
      <div class="sc-value" style="font-size:15px">${f(firstD)} <span style="color:#9ca3af">→</span> ${f(lastD)}</div>
      <div class="sc-sub">첫 기록부터 최근까지</div></div>`);
  }
  box.innerHTML = cards.join("");
  $("statsCardHead").style.display = cards.length ? "" : "none";
}

// ---- 가볼 만한 곳 추천 ----
// CLIMATE: 지역 코드 -> [[월평균기온, 월강수량], x12] (2020~2024 실측 평년값, 빌드 때 미리 받아둠)
let recMonth = new Date().getMonth();      // 0-11

// 추천 점수 항목별 비중. 자료가 없는 항목은 0점 처리하지 않고 빼낸 뒤
// 남은 비중으로 다시 나눈다 — 인기도처럼 일부 지역만 자료가 있는 항목 때문에
// 자료 없는 지역이 부당하게 밀리는 것을 막기 위함.
// (지역 분산은 점수가 아니라 시·도당 노출 개수 제한으로 처리한다)
const REC_WEIGHTS = { weather: 0.35, season: 0.25, popular: 0.20, taste: 0.10 };
const festivalsByCode = FESTIVALS.reduce((byCode, festival) => {
  (byCode[festival.c] ||= []).push(festival);
  return byCode;
}, {});

// 여행하기 좋은 날씨 점수 — 기온이 18도에 가까울수록, 비가 적을수록 높다
function climateScore(t, p) {
  if (t == null || p == null) return null;
  const warmth = Math.max(0, 100 - Math.abs(t - 18) * 6);
  const dry = Math.max(0, 100 - Math.max(0, p - 60) * 0.35);
  return Math.round(warmth * 0.65 + dry * 0.35);
}

// 그 달에 축제가 있으면 높게. 없다고 0점은 아니라서 기본값을 둔다.
function seasonScore(code, month) {
  const fs = festivalsByCode[code] || [];
  if (!fs.length) return 30;
  if (fs.some(f => f.m.includes(month))) return 100;
  const near = (month % 12) + 1, prev = ((month + 10) % 12) + 1;
  return fs.some(f => f.m.includes(near) || f.m.includes(prev)) ? 55 : 35;
}

// 지금까지 다닌 곳에서 취향을 읽는다 (지역 유형 + 자주 간 시·도).
// 기록이 적으면 신호가 아니라 잡음이라 아예 항목에서 뺀다.
function tasteProfile() {
  const mine = [...visited].filter(c => nameByCode[c] && countryOfCode(c) === COUNTRY);
  if (mine.length < 6) return null;      // 몇 곳 안 가봤으면 취향이라 할 만한 게 없다
  const kind = {}, prov = {};
  mine.forEach(c => {
    const k = (nameByCode[c] || "").slice(-1);      // 시 / 군 / 구
    kind[k] = (kind[k] || 0) + 1;
    const g = groupOf(c);
    prov[g] = (prov[g] || 0) + 1;
  });
  return { kind, prov, n: mine.length };
}
function tasteScore(code, tp) {
  if (!tp) return null;
  const k = (nameByCode[code] || "").slice(-1);
  const kindShare = (tp.kind[k] || 0) / tp.n;             // 같은 유형을 얼마나 좋아하나
  const provShare = (tp.prov[groupOf(code)] || 0) / tp.n; // 그 시·도를 얼마나 자주 갔나
  // 한 지역만 계속 밀지 않도록 시·도 쏠림은 상한을 둔다
  return Math.round(Math.min(1, kindShare * 0.6 + Math.min(provShare, 0.4) * 1.0) * 100);
}

// 항목별 점수를 비중에 맞춰 합산하되, 자료 없는 항목은 비중째로 제외
function recScore(parts) {
  let sum = 0, w = 0;
  for (const key in REC_WEIGHTS) {
    if (parts[key] == null) continue;
    sum += parts[key] * REC_WEIGHTS[key];
    w += REC_WEIGHTS[key];
  }
  return w ? Math.round(sum / w) : null;
}
function climateWhy(t, p) {
  const heat = t >= 26 ? "덥지만" : t >= 20 ? "따뜻하고" : t >= 12 ? "선선하고" : t >= 4 ? "쌀쌀하고" : "춥지만";
  const wet = p >= 200 ? "비가 많아요" : p >= 110 ? "비가 잦은 편" : p >= 55 ? "비는 보통" : "비가 적어요";
  return `${heat} ${wet}`;
}

function buildRecommendationRows() {
  const rows = [];
  let unvisited = 0, haveData = 0;
  const tp = tasteProfile();
  const month = recMonth + 1;
  Object.keys(nameByCode).forEach(code => {
    if (countryOfCode(code) !== COUNTRY) return;
    const c = CLIMATE[code];
    if (c && c[recMonth]) haveData++;
    if (visited.has(code)) return;
    unvisited++;
    if (!c || !c[recMonth]) return;
    const [t, p] = c[recMonth];
    const weather = climateScore(t, p);
    if (weather == null) return;
    const hot = TRENDING[code];
    const parts = {
      weather,
      season: seasonScore(code, month),
      popular: hot ? hot.s : null,
      taste: tasteScore(code, tp),
    };
    rows.push({ code, t, p, parts, hot, s: recScore(parts) });
  });
  rows.sort((a, b) => b.s - a.s || a.code.localeCompare(b.code));
  return { rows, unvisited, haveData, month };
}

function takeDiverseRecommendations(rows, limit = 12) {
  const perProv = {}, top = [];
  for (const row of rows) {
    const group = groupOf(row.code);
    if ((perProv[group] = (perProv[group] || 0) + 1) > 2) continue;
    top.push(row);
    if (top.length === limit) break;
  }
  return top;
}

function recommendationReasons(row, month) {
  const why = [];
  if (row.parts.weather >= 70) why.push(`${month}월 날씨 좋음`);
  if (row.parts.season >= 100) why.push("이달 축제");
  else if (row.parts.season >= 55) why.push("축제 시즌 근접");
  if (row.hot) why.push(escapeHtml(row.hot.t));
  if (row.parts.taste != null && row.parts.taste >= 70) why.push("취향과 비슷");
  return why.length ? why : [climateWhy(row.t, row.p)];
}

function recommendationCard(row, month) {
  const emblem = EMBLEMS[row.code];
  return `<div class="rec-card" data-code="${row.code}">
    ${emblem ? `<img src="${emblem}" alt="" loading="lazy">` : ""}
    <div class="rec-main">
      <div class="rec-name">${escapeHtml(nameByCode[row.code])}</div>
      <div class="rec-why">${PROV[groupOf(row.code)] || ""} · ${recommendationReasons(row, month).join(" · ")}</div>
    </div>
    <div class="rec-num"><b>${row.t}°</b>${row.p}mm</div>
  </div>`;
}

function renderRecs() {
  const list = $("recList"), note = $("recNote"), sel = $("recMonth");
  if (!list) return;
  if (!sel.options.length) {
    sel.innerHTML = Array.from({ length: 12 }, (_, i) => `<option value="${i}">${i + 1}월</option>`).join("");
    sel.value = recMonth;
  }
  // 같은 시·도가 몰리면 목록이 단조로워진다 (대구 자치구만 4개 같은 식). 시·도당 2곳까지.
  const { rows, unvisited, haveData, month } = buildRecommendationRows();
  const top = takeDiverseRecommendations(rows);
  const cName = COUNTRY === "jp" ? "일본" : "한국";
  // 결과가 없는 이유를 구분해서 알린다 (다 가본 것과 날씨 자료가 없는 것은 다르다)
  const at = TRENDING_AT ? `인기도는 ${TRENDING_AT} 기준 편집 자료입니다.` : "";
  note.textContent = rows.length
    ? `${cName}에서 아직 안 가본 ${rows.length}곳 중 ${month}월 날씨·축제·인기·취향을 함께 본 순서입니다. ${at}`
    : !haveData ? `${cName}은 아직 날씨 자료가 없습니다.`
    : !unvisited ? `${cName}은 이미 다 다녀오셨네요.`
    : `추천할 곳을 찾지 못했습니다.`;
  list.innerHTML = top.map(row => recommendationCard(row, month)).join("");
  list.querySelectorAll(".rec-card").forEach(el => {
    el.onclick = () => { setTab("map"); focusRegion(el.dataset.code); };
  });
  renderFestivals();
}

// 축제 기간 표시 — 실제 일정이 있으면 날짜로, 없으면 달로.
// 해마다 일정이 바뀌므로 지난해 자료는 연도를 붙여 지난 일정임을 밝힌다.
function festivalWhen(f) {
  const span = f.m.length > 1 ? `${f.m[0]}~${f.m[f.m.length - 1]}월` : `${f.m[0]}월`;
  if (!f.s || !f.e) return span;
  const y = +f.s.slice(0, 4), m1 = +f.s.slice(4, 6), d1 = +f.s.slice(6, 8);
  const m2 = +f.e.slice(4, 6), d2 = +f.e.slice(6, 8);
  const body = m1 === m2
    ? (d1 === d2 ? `${m1}.${d1}` : `${m1}.${d1}~${d2}`)
    : `${m1}.${d1}~${m2}.${d2}`;
  const thisYear = new Date().getFullYear();
  return y < thisYear ? `${y}년 ${body}` : body;
}

// 이달의 축제 — 매년 반복되는 행사라 '달'로만 다룬다 (정확한 일정은 해마다 바뀜)
function renderFestivals() {
  const list = $("fesList"), note = $("fesNote"), head = $("fesHead");
  if (!list) return;
  const m = recMonth + 1;
  const rows = FESTIVALS.filter(f => f.m.includes(m) && countryOfCode(f.c) === COUNTRY
                                     && nameByCode[f.c]);
  head.textContent = `${m}월의 축제`;
  const cName = COUNTRY === "jp" ? "일본" : "한국";
  const dated = rows.filter(f => f.s).length;
  note.textContent = rows.length
    ? (dated
        ? `날짜는 한국관광공사 자료 기준입니다. 해마다 달라지니 방문 전 확인하세요.`
        : "해마다 열리는 대표 축제입니다. 정확한 일정은 해마다 달라지니 확인 후 방문하세요.")
    : `${m}월에 등록된 ${cName} 축제가 없습니다.`;
  list.innerHTML = rows.map(f => {
    const span = festivalWhen(f);
    // 사진이 있으면 왼쪽에, 없으면 태그 배지로 (사진은 절반 정도만 있다)
    const lead = f.p
      ? `<img class="fes-photo" src="${f.p}" alt="" loading="lazy" decoding="async"
              title="${escapeHtml(f.cr || "")}">`
      : `<span class="fes-tag">${escapeHtml(f.t)}</span>`;
    return `<div class="fes-card${visited.has(f.c) ? " done" : ""}" data-code="${f.c}">
      ${lead}
      <div class="fes-main">
        <div class="fes-name">${escapeHtml(f.n)}</div>
        <div class="fes-sub">${escapeHtml(nameByCode[f.c])} · ${escapeHtml(f.d)}</div>
      </div>
      <span class="fes-when">${span}</span>
    </div>`;
  }).join("");
  list.querySelectorAll(".fes-card").forEach(el => {
    el.onclick = () => { setTab("map"); focusRegion(el.dataset.code); };
  });
}

// 기록 검색·기간 필터 상태 (지도 상태가 아니라 화면 조건이라 서버에 저장하지 않는다)
let feedQuery = "", feedYear = "";
const visitYear = v => (v.start || v.end || "").slice(0, 4);

// 연도 목록은 지금 나라의 기록에서 뽑는다 (나라를 바꾸면 다시 채워짐)
function syncYearOptions() {
  const sel = $("feedYear");
  if (!sel) return;
  const years = new Set();
  Object.keys(notes).forEach(code => {
    if (!nameByCode[code] || countryOfCode(code) !== COUNTRY) return;
    getVisits(code).forEach(v => { const y = visitYear(v); if (y) years.add(y); });
  });
  const list = [...years].sort((a, b) => b.localeCompare(a));
  if (sel.dataset.years === list.join(",")) return;    // 바뀐 게 없으면 그대로
  sel.dataset.years = list.join(",");
  // 지금 고른 연도가 사라졌으면 전체로 되돌린다
  if (feedYear && !years.has(feedYear)) feedYear = "";
  sel.innerHTML = `<option value="">전체 기간</option>` +
    list.map(y => `<option value="${y}">${y}년</option>`).join("");
  sel.value = feedYear;
}

function renderFeed() {
  renderStatsCards();
  renderRecs();
  const feed = $("memoryFeed");
  if (!feed) return;
  syncYearOptions();
  const q = feedQuery.trim().toLowerCase();
  const entries = [];
  let totalCodes = 0;
  const codes = new Set([...Object.keys(notes), ...Object.keys(photos)]);
  codes.forEach(code => {
    if (!nameByCode[code]) return;   // 지도에 없는 코드는 건너뜀
    if (countryOfCode(code) !== COUNTRY) return;   // 현재 나라의 기록만
    let visits = getVisits(code).filter(v => v.start || v.end || v.memo);
    let ph = photos[code] || [];
    if (!visits.length && ph.length === 0) return;
    totalCodes++;
    // 기간 필터: 해당 연도의 방문만 남기고, 남는 게 없으면 카드를 빼낸다
    if (feedYear) {
      visits = visits.filter(v => visitYear(v) === feedYear);
      if (!visits.length) return;
      const vids = new Set(visits.map(v => v.id));
      ph = ph.filter(p => p.vid && vids.has(p.vid));
    }
    // 검색: 지역명·시도명·메모에서 찾는다
    if (q) {
      const hay = [nameByCode[code], PROV[groupOf(code)] || "",
                   ...visits.map(v => v.memo || "")].join(" ").toLowerCase();
      if (!hay.includes(q)) return;
    }
    // 최신 방문이 위로
    visits.sort((a, b) => (b.start || b.end || "0").localeCompare(a.start || a.end || "0"));
    entries.push({ code, visits, photos: ph, latest: (visits[0] && (visits[0].start || visits[0].end)) || "0" });
  });
  entries.sort((a, b) => b.latest.localeCompare(a.latest));
  const filtering = !!(q || feedYear);
  $("feedFilter").style.display = totalCodes > 1 || filtering ? "" : "none";
  const empty = $("feedEmpty");
  empty.style.display = filtering && !entries.length ? "" : "none";
  empty.textContent = "조건에 맞는 기록이 없습니다.";
  feed.innerHTML = entries.map(e => {
    const emblem = EMBLEMS[e.code];
    const prov = PROV[groupOf(e.code)] || "";
    const name = nameByCode[e.code] || "";
    const countHtml = e.visits.length > 1
      ? `<span class="mem-count">${e.visits.length}번 방문</span>` : "";
    const visitsHtml = e.visits.map(v => {
      const d = visitDateText(v);
      const vp = e.photos.filter(p => (p.vid || "") === v.id);
      return `<div class="mem-visit">
        ${d ? `<span class="mem-vdate">${d}</span>` : ""}
        ${v.memo ? `<span class="mem-vmemo">${escapeHtml(v.memo)}</span>` : ""}
      </div>` + (vp.length
        ? `<div class="mem-photos">${vp.map(p =>
            `<img src="${thumbOf(p)}" loading="lazy" decoding="async" data-full="${p.url}">`).join("")}</div>`
        : "");
    }).join("");
    // 방문에 속하지 않은 사진은 맨 아래
    const vids = new Set(e.visits.map(v => v.id));
    const rest = e.photos.filter(p => !p.vid || !vids.has(p.vid));
    const photosHtml = rest.length
      ? `<div class="mem-photos">${rest.map(p =>
          `<img src="${thumbOf(p)}" loading="lazy" decoding="async" data-full="${p.url}">`).join("")}</div>`
      : "";
    return `<div class="mem-card" data-code="${e.code}">
      <div class="mem-head">
        ${emblem ? `<img src="${emblem}" alt="">` : ""}
        <div><div class="mem-title">${name}</div><div class="mem-prov">${prov}</div></div>
        ${countHtml}
      </div>${visitsHtml}${photosHtml}</div>`;
  }).join("");
  // 사진 클릭은 확대 (지역 이동 아님) — 같은 지역 카드의 사진끼리 넘겨보기
  feed.querySelectorAll(".mem-card").forEach(card => {
    const urls = [...card.querySelectorAll("img[data-full]")].map(i => i.dataset.full);
    card.querySelectorAll("img[data-full]").forEach(img => {
      img.onclick = (ev) => {
        ev.stopPropagation();
        openLightbox(urls, img.dataset.full);
      };
    });
  });
  feed.querySelectorAll(".mem-card").forEach(card => {
    card.onclick = () => focusRegion(card.dataset.code);
  });
}
function escapeHtml(s) {
  return s.replace(/[&<>"]/g, c => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c]));
}

// 첫 방문 안내: 방이 비어 있고, 이 방에서 아직 안내를 닫지 않았으면 표시
function maybeShowFirstHint() {
  const seenKey = "hintSeen:" + ROOM;
  const empty = visited.size === 0 && Object.keys(photos).length === 0;
  if (empty && !localStorage.getItem(seenKey)) $("firstHint").style.display = "block";
}
$("firstHintClose").onclick = () => {
  $("firstHint").style.display = "none";
  localStorage.setItem("hintSeen:" + ROOM, "1");
};
// 지역을 한 번 누르면 안내 자동 숨김
function dismissFirstHint() {
  if ($("firstHint").style.display === "block") {
    $("firstHint").style.display = "none";
    localStorage.setItem("hintSeen:" + ROOM, "1");
  }
}

// 사진 압축: 원본은 1600px JPEG, 목록용은 더 작은 WebP로 저장한다.
async function compressImage(file, max = 1600, quality = 0.85, outputType = "image/jpeg") {
  const sourceName = file.name || "photo";
  if (!/^image\//.test(file.type) && file.type !== "application/octet-stream") {
    return { blob: file, name: sourceName };
  }
  const img = await createImageBitmap(file).catch(() => null);
  if (!img) return { blob: file, name: sourceName };
  const k = Math.min(1, max / Math.max(img.width, img.height));
  if (k === 1 && file.size < 2 * 1024 * 1024 && max >= 1600) {
    img.close?.();
    return { blob: file, name: sourceName };
  }
  const c = document.createElement("canvas");
  c.width = Math.round(img.width * k);
  c.height = Math.round(img.height * k);
  c.getContext("2d").drawImage(img, 0, 0, c.width, c.height);
  img.close?.();
  const blob = await new Promise(r => c.toBlob(r, outputType, quality));
  if (!blob) return { blob: file, name: sourceName };
  const ext = outputType === "image/webp" ? ".webp" : ".jpg";
  return { blob, name: sourceName.replace(/\.\w+$/, "") + ext };
}
// 목록에 쓸 작은 사진. 앨범 칸은 60px 남짓인데 원본(1600px)을 내려받으면
// 사진 한 장에 수백 KB가 들어 모바일에서 한참 걸린다.
const thumbOf = p => p.thumb || p.url;

// 썸네일이 도입되기 전에 올린 사진은 목록에서도 원본(수백 KB)을 받는다.
// 지도를 열었을 때 그런 사진을 찾아 조용히 썸네일을 만들어 올려둔다.
// 한 번만 하면 되고, 실패해도 원본으로 계속 보이므로 조용히 넘어간다.
let thumbFillRan = false;
async function backfillThumbs() {
  if (thumbFillRan || !ROOM) return;
  thumbFillRan = true;
  const todo = [];
  for (const [code, list] of Object.entries(photos)) {
    for (const p of list || []) if (p.url && !p.thumb) todo.push({ code, p });
  }
  if (!todo.length) return;
  for (const { code, p } of todo.slice(0, 40)) {     // 한 번에 너무 많이 하지 않는다
    try {
      const res = await fetch(p.url);
      if (!res.ok) continue;
      const file = new File([await res.blob()], (p.name || "photo.jpg"), { type: "image/jpeg" });
      const t = await compressImage(file, 400, 0.7);
      const r = await api(`/api/photo?room=${ROOM}&code=${code}&thumbfor=${encodeURIComponent(p.url)}`, {
        method: "POST",
        headers: { "Content-Type": "application/octet-stream", "X-Filename": encodeURIComponent(t.name) },
        body: t.blob
      });
      p.thumb = r.thumb;
    } catch { /* 원본으로 계속 보이므로 넘어간다 */ }
  }
  renderFeed();
  if (selected) renderPanel();
}

// 썸네일 기능 도입 전에 올린 사진은 원본이 한 번 표시된 뒤, 유휴 시간에 작은 썸네일을 만든다.
// 실제로 화면에 나타난 사진만 한 장씩 처리해 첫 화면 네트워크를 방해하지 않는다.
const thumbnailQueue = [];
const queuedThumbnailUrls = new Set();
let thumbnailWorkerRunning = false;
function resetThumbnailQueue() {
  thumbnailQueue.length = 0;
  queuedThumbnailUrls.clear();
}
function findPhotoRecord(url) {
  for (const [code, list] of Object.entries(photos)) {
    const photo = (list || []).find(p => p.url === url);
    if (photo) return { code, photo };
  }
  return null;
}
function waitForIdle() {
  return new Promise(resolve => {
    if ("requestIdleCallback" in window) requestIdleCallback(() => resolve(), { timeout: 1500 });
    else setTimeout(resolve, 200);
  });
}
function queueMissingThumbnail(url) {
  const found = findPhotoRecord(url);
  if (!ROOM || !navigator.onLine || !found || found.photo.thumb || queuedThumbnailUrls.has(url)) return;
  queuedThumbnailUrls.add(url);
  thumbnailQueue.push({ room: ROOM, code: found.code, url });
  processThumbnailQueue();
}
async function processThumbnailQueue() {
  if (thumbnailWorkerRunning) return;
  thumbnailWorkerRunning = true;
  while (thumbnailQueue.length) {
    const job = thumbnailQueue.shift();
    try {
      if (job.room !== ROOM) continue;
      await waitForIdle();
      if (job.room !== ROOM) continue;
      const current = findPhotoRecord(job.url);
      if (!current || current.photo.thumb) continue;
      const response = await fetch(job.url, { cache: "force-cache" });
      if (!response.ok) continue;
      const source = await response.blob();
      if (!/^image\//.test(source.type) && source.type !== "application/octet-stream") continue;
      const t = await compressImage(source, 240, 0.68, "image/webp");
      if (job.room !== ROOM) continue;
      markDirty(job.code);
      const saved = await api(`/api/photo?room=${job.room}&code=${job.code}&thumbfor=${encodeURIComponent(job.url)}`, {
        method: "POST",
        headers: { "Content-Type": "application/octet-stream", "X-Filename": encodeURIComponent(t.name) },
        body: t.blob
      });
      const live = findPhotoRecord(job.url);
      if (!live) continue;
      live.photo.thumb = saved.thumb;
      document.querySelectorAll("img[data-full]").forEach(img => {
        if (img.dataset.full === job.url) img.src = saved.thumb;
      });
      cacheState(ROOM, { name: $("roomName").textContent, visited: [...visited], photos, notes });
    } catch {
      // 원본 CORS나 일시적인 네트워크 오류가 있으면 원본 표시를 유지한다.
    } finally {
      queuedThumbnailUrls.delete(job.url);
    }
  }
  thumbnailWorkerRunning = false;
}
document.addEventListener("load", e => {
  const img = e.target;
  if (!(img instanceof HTMLImageElement) || !img.dataset.full) return;
  if (img.getAttribute("src") === img.dataset.full) queueMissingThumbnail(img.dataset.full);
}, true);

$("photoInput").addEventListener("change", async e => {
  if (!selected) { e.target.value = ""; return; }
  const code = selected, vid = pendingVid;
  markDirty(code);
  const files = [...e.target.files];
  e.target.value = "";
  for (let i = 0; i < files.length; i++) {
    $("uploadMsg").textContent = `업로드 중… (${i + 1}/${files.length})`;
    try {
      const { blob, name } = await compressImage(files[i]);
      const j = await api(`/api/photo?room=${ROOM}&code=${code}&vid=${encodeURIComponent(vid)}`, {
        method: "POST",
        headers: { "Content-Type": "application/octet-stream", "X-Filename": encodeURIComponent(name) },
        body: blob
      });
      const entry = { url: j.url, name: j.name, vid: j.vid || vid };
      (photos[code] = photos[code] || []).push(entry);
      // 목록용 작은 사진은 실패해도 원본으로 대체되므로 조용히 넘어간다
      try {
        const t = await compressImage(files[i], 240, 0.68, "image/webp");
        const r = await api(`/api/photo?room=${ROOM}&code=${code}&thumbfor=${encodeURIComponent(j.url)}`, {
          method: "POST",
          headers: { "Content-Type": "application/octet-stream", "X-Filename": encodeURIComponent(t.name) },
          body: t.blob
        });
        entry.thumb = r.thumb;
      } catch {}
    } catch { alert(`${files[i].name} 업로드 실패`); }
  }
  $("uploadMsg").textContent = "";
  // 사진만 올린 방문 기록도 서버에 남도록 저장
  if (vid) saveVisits(code, getVisits(code));
  else if ((photos[code] || []).length && !visited.has(code)) toggleVisited(code);
  renderPanel(); renderFeed();
});
// ---- 사진 크게 보기 (넘기기 + 확대/축소) ----
let lbList = [], lbIdx = 0;
let lbResetZoom = () => {};   // 확대 상태 초기화 (아래 확대 모듈에서 채움)
function openLightbox(urls, startUrl) {
  lbList = (urls && urls.length) ? urls : [startUrl];
  lbIdx = Math.max(0, lbList.indexOf(startUrl));
  const box = $("lightbox");
  box.classList.toggle("multi", lbList.length > 1);
  lbResetZoom();
  showLbPhoto();
  box.style.display = "flex";
}
function showLbPhoto() {
  const box = $("lightbox");
  box.querySelector("img").src = lbList[lbIdx];
  $("lbCount").textContent = `${lbIdx + 1} / ${lbList.length}`;
}
function lbMove(step) {
  if (lbList.length < 2) return;
  lbIdx = (lbIdx + step + lbList.length) % lbList.length;
  lbResetZoom();   // 사진을 넘기면 확대 해제
  showLbPhoto();
}
function closeLightbox() { lbResetZoom(); $("lightbox").style.display = "none"; }
$("lbPrev").onclick = (e) => { e.stopPropagation(); lbMove(-1); };
$("lbNext").onclick = (e) => { e.stopPropagation(); lbMove(1); };
document.addEventListener("keydown", (e) => {
  if ($("lightbox").style.display !== "flex") return;
  const zoomed = $("lightbox").classList.contains("zoomed");
  if (e.key === "Escape") { if (zoomed) lbResetZoom(); else closeLightbox(); }
  else if (e.key === "ArrowLeft") { if (!zoomed) lbMove(-1); }
  else if (e.key === "ArrowRight") { if (!zoomed) lbMove(1); }
});
// ---- 확대/축소 + 스와이프 ----
// 확대 안 했을 때: 좌우 스와이프=사진 넘기기, 위아래로 크게 밀면 닫기
// 확대했을 때: 드래그=화면 이동, 넘기기 비활성 (핀치·더블탭·휠로 배율 조절)
(() => {
  const box = $("lightbox");
  const img = box.querySelector("img");
  const MAXZ = 5;
  let scale = 1, tx = 0, ty = 0;
  const pts = new Map();
  let startDist = 0, startScale = 1, startMid = null, startTx = 0, startTy = 0;
  let dragStart = null, moved = false, lastTap = null;

  function apply() {
    img.style.transform = `translate(${tx}px, ${ty}px) scale(${scale})`;
    box.classList.toggle("zoomed", scale > 1.01);
  }
  function clampPan() {
    // 확대된 이미지가 화면 밖으로 너무 빠져나가지 않게
    const r = img.getBoundingClientRect();
    const w = r.width / scale, h = r.height / scale;
    const maxX = Math.max(0, (w * scale - innerWidth) / 2 + 20);
    const maxY = Math.max(0, (h * scale - innerHeight) / 2 + 20);
    tx = Math.min(maxX, Math.max(-maxX, tx));
    ty = Math.min(maxY, Math.max(-maxY, ty));
  }
  function resetZoom() { scale = 1; tx = ty = 0; apply(); }
  lbResetZoom = resetZoom;    // 사진을 넘기거나 닫을 때 초기화

  function zoomAtPoint(nextScale, cx, cy) {
    const prev = scale;
    scale = Math.min(MAXZ, Math.max(1, nextScale));
    // 손가락(또는 커서) 위치를 기준으로 확대되도록 이동값 보정
    const k = scale / prev;
    tx = cx - (cx - tx) * k;
    ty = cy - (cy - ty) * k;
    if (scale === 1) { tx = ty = 0; } else clampPan();
    apply();
  }

  box.addEventListener("pointerdown", (e) => {
    if (e.target.closest(".lb-nav")) return;
    pts.set(e.pointerId, e);
    if (pts.size === 2) {
      const [a, b] = [...pts.values()];
      startDist = Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY);
      startScale = scale;
      startMid = { x: (a.clientX + b.clientX) / 2 - innerWidth / 2,
                   y: (a.clientY + b.clientY) / 2 - innerHeight / 2 };
      startTx = tx; startTy = ty;
      dragStart = null;
    } else if (pts.size === 1) {
      dragStart = { x: e.clientX, y: e.clientY, tx, ty };
      moved = false;
    }
    try { box.setPointerCapture(e.pointerId); } catch {}
  });

  box.addEventListener("pointermove", (e) => {
    if (!pts.has(e.pointerId)) return;
    pts.set(e.pointerId, e);
    if (pts.size === 2 && startDist > 0) {
      const [a, b] = [...pts.values()];
      const d = Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY);
      const next = startScale * (d / startDist);
      const prev = scale;
      scale = Math.min(MAXZ, Math.max(1, next));
      const k = scale / startScale;
      tx = startTx + startMid.x * (1 - k);
      ty = startTy + startMid.y * (1 - k);
      if (scale === 1) { tx = ty = 0; } else clampPan();
      apply();
      moved = true;
    } else if (pts.size === 1 && dragStart) {
      const dx = e.clientX - dragStart.x, dy = e.clientY - dragStart.y;
      if (Math.abs(dx) > 6 || Math.abs(dy) > 6) moved = true;
      if (scale > 1.01) {   // 확대 상태에서만 드래그로 이동
        tx = dragStart.tx + dx; ty = dragStart.ty + dy;
        clampPan(); apply();
      }
    }
  });

  function endPointer(e) {
    if (!pts.has(e.pointerId)) return;
    const wasSingle = pts.size === 1;
    const start = dragStart;
    pts.delete(e.pointerId);
    if (pts.size < 2) startDist = 0;
    if (!wasSingle || !start) { dragStart = null; return; }
    dragStart = null;
    const dx = e.clientX - start.x, dy = e.clientY - start.y;

    if (scale <= 1.01) {
      // 확대 안 한 상태: 스와이프로 넘기기 / 아래위로 크게 밀면 닫기
      if (Math.abs(dx) > 45 && Math.abs(dx) > Math.abs(dy)) { lbMove(dx < 0 ? 1 : -1); return; }
      if (Math.abs(dy) > 90 && Math.abs(dy) > Math.abs(dx)) { closeLightbox(); return; }
    }
    // 더블탭(또는 더블클릭): 확대 ↔ 원래대로
    if (!moved) {
      const now = Date.now();
      const near = lastTap && Math.hypot(e.clientX - lastTap.x, e.clientY - lastTap.y) < 40;
      if (lastTap && now - lastTap.t < 450 && near) {
        lastTap = null;
        const cx = e.clientX - innerWidth / 2, cy = e.clientY - innerHeight / 2;
        if (scale > 1.01) resetZoom(); else zoomAtPoint(2.5, cx, cy);
      } else {
        lastTap = { t: now, x: e.clientX, y: e.clientY };
        // 배경(이미지 밖) 단일 탭은 닫기 — 단, 더블탭일 수 있으니 잠깐 기다렸다 실행
        if (scale <= 1.01 && e.target.id === "lightbox") {
          const mine = lastTap;
          setTimeout(() => {
            if (lastTap === mine && scale <= 1.01) closeLightbox();   // 두 번째 탭이 없었으면 닫기
          }, 300);
        }
      }
    }
  }
  box.addEventListener("pointerup", endPointer);
  box.addEventListener("pointercancel", (e) => { pts.delete(e.pointerId); dragStart = null; startDist = 0; });

  // 데스크톱: 휠로 확대/축소
  box.addEventListener("wheel", (e) => {
    e.preventDefault();
    const cx = e.clientX - innerWidth / 2, cy = e.clientY - innerHeight / 2;
    zoomAtPoint(scale * (e.deltaY > 0 ? 1 / 1.2 : 1.2), cx, cy);
  }, { passive: false });
})();

// ---- 툴팁 (두 나라 지도 모두) ----
Object.values(MAPS).forEach(m => m.paths.forEach(p => {
  p.addEventListener("pointermove", e => {
    const code = p.dataset.code;
    const prov = PROV[groupOf(code)] || "";
    const mark = visited.has(code) ? ' <span class="v">✓ 방문</span>' : "";
    const ph = (photos[code] || []).length;
    tooltip.innerHTML = `${prov} ${p.dataset.name}${mark}${ph ? ` · 사진 ${ph}` : ""}`;
    tooltip.style.display = "block";
    tooltip.style.left = (e.clientX + 14) + "px";
    tooltip.style.top = (e.clientY + 14) + "px";
  });
  p.addEventListener("pointerleave", () => { tooltip.style.display = "none"; });
}));

// ---- 통계 (현재 보고 있는 나라 기준) ----
let provCodes = [], provTotals = {};
function buildProvList() {
  provTotals = {};
  paths.forEach(p => {
    const pc = groupOf(p.dataset.code);
    provTotals[pc] = (provTotals[pc] || 0) + 1;
  });
  provCodes = Object.keys(provTotals).sort();
  $("total").textContent = paths.length;
  $("statsHead").textContent = COUNTRY === "jp" ? "지방별 달성률" : "시·도별 달성률";
  $("unitLabel").textContent = COUNTRY === "jp" ? "도도부현" : "시·군·구";
  $("provList").innerHTML = provCodes.map(pc =>
    `<div class="prov-row" id="prov-${pc}"><span class="name">${PROV[pc] || ""}</span>` +
    `<div class="bar"><div></div></div><span class="num"></span></div>`).join("");
}
function render() {
  // 방문 수는 현재 나라 기준으로만 집계
  let n = 0;
  visited.forEach(c => { if (countryOfCode(c) === COUNTRY && nameByCode[c]) n++; });
  $("cnt").textContent = n;
  const pct = paths.length ? n / paths.length * 100 : 0;
  const pctText = (pct > 0 && pct < 10 ? pct.toFixed(1) : Math.round(pct)) + "%";
  $("pct").textContent = pctText;
  $("totalBar").style.width = pct + "%";
  $("mapStatPill").innerHTML = `<b>${n}</b> / ${paths.length} · ${pctText}`;
  const pv = {};
  visited.forEach(c => { if (!nameByCode[c]) return; const pc = groupOf(c); pv[pc] = (pv[pc] || 0) + 1; });
  provCodes.forEach(pc => {
    const row = $("prov-" + pc);
    if (!row) return;
    const v = pv[pc] || 0, t = provTotals[pc] || 0;
    row.querySelector(".bar > div").style.width = (t ? v / t * 100 : 0) + "%";
    row.querySelector(".num").textContent = `${v}/${t}`;
    row.classList.toggle("done", v === t && t > 0);
  });
}
buildProvList();

// ---- 확대/이동 + 라벨 표시 ----
function applyVB() {
  svg.setAttribute("viewBox", `${vb.x} ${vb.y} ${vb.w} ${vb.h}`);
  updateLabels();
}
// 나라 전환
function setCountry(cc) {
  if (!MAPS[cc] || cc === COUNTRY) return;
  bindMap(cc);
  Object.values(MAPS).forEach(m => m.svg.classList.toggle("hidden", m.cc !== cc));
  document.querySelectorAll(".cswitch button").forEach(b =>
    b.classList.toggle("active", b.dataset.country === cc));
  // 선택 해제 후 활성 지도 기준으로 다시 그림
  selected = null;
  $("rpHint").style.display = "";
  $("rpBody").style.display = "none";
  if (mq.matches) regionPanelEl.classList.remove("open");
  buildProvList();
  render(); renderFeed(); applyVB();
}
document.querySelectorAll(".cswitch button").forEach(b => {
  b.onclick = () => setCountry(b.dataset.country);
});
function regionBBox(code) {
  // 숨겨진 상태(getBBox=0)는 캐시하지 않아 나중에 다시 정확히 측정
  if ((!bboxCache[code] || !bboxCache[code].width) && pathByCode[code]) {
    const bb = pathByCode[code].getBBox();
    if (bb.width) bboxCache[code] = bb;
    else return bb;
  }
  return bboxCache[code];
}
// 앨범 카드 → 해당 지역으로 지도 이동 + 선택
function focusRegion(code) {
  setTab("map");
  setCountry(countryOfCode(code));   // 다른 나라 지역이면 지도부터 전환
  const bb = regionBBox(code);
  if (bb && bb.width) {
    const cx = bb.x + bb.width / 2, cy = bb.y + bb.height / 2;
    const ar = home.w / home.h;
    // 지역이 화면의 약 45%를 차지하도록 여백 포함 크기 계산
    let w = Math.max(bb.width, bb.height * ar) / 0.45;
    w = Math.min(Math.max(w, home.w / 18), home.w);
    let h = w / ar;
    vb = { x: cx - w / 2, y: cy - h / 2, w, h };
    applyVB();
  }
  select(code);
}

// ---- 지역 검색 (한국·일본 모두) ----
const searchIndex = Object.values(MAPS).flatMap(m => m.paths.map(p => ({
  code: p.dataset.code,
  name: p.dataset.name,
  prov: PROV[groupOf(p.dataset.code)] || "",
  cc: m.cc
})));
const searchInput = $("searchInput");
const searchResults = $("searchResults");
let srActive = -1, srItems = [];

function runSearch(q) {
  q = q.trim();
  if (!q) { closeSearch(); return; }
  const scored = [];
  for (const r of searchIndex) {
    const full = r.prov + " " + r.name;
    let score = -1;
    if (r.name.startsWith(q)) score = 0;
    else if (r.name.includes(q)) score = 1;
    else if (full.includes(q)) score = 2;
    if (score >= 0) scored.push({ ...r, score });
  }
  scored.sort((a, b) => (a.cc === COUNTRY ? 0 : 1) - (b.cc === COUNTRY ? 0 : 1)
    || a.score - b.score || a.name.localeCompare(b.name));
  srItems = scored.slice(0, 12);
  srActive = srItems.length ? 0 : -1;
  if (!srItems.length) {
    searchResults.innerHTML = `<div class="sr-empty">"${escapeHtml(q)}" 검색 결과가 없어요</div>`;
  } else {
    searchResults.innerHTML = srItems.map((r, i) => {
      const em = EMBLEMS[r.code];
      const vis = visited.has(r.code) ? `<span class="sr-visited">✓</span>` : "";
      return `<div class="sr-item${i === srActive ? " active" : ""}" data-code="${r.code}" data-i="${i}">
        ${em ? `<img src="${em}" alt="">` : ""}
        <span>${escapeHtml(r.name)}</span> ${vis}
        <span class="sr-prov">${r.cc === "jp" ? "🇯🇵 " : ""}${r.prov}</span>
      </div>`;
    }).join("");
    searchResults.querySelectorAll(".sr-item").forEach(el => {
      el.onmousedown = (e) => { e.preventDefault(); pickSearch(el.dataset.code); };
      el.onmouseenter = () => { srActive = +el.dataset.i; highlightSR(); };
    });
  }
  searchResults.classList.add("show");
}
function highlightSR() {
  searchResults.querySelectorAll(".sr-item").forEach((el, i) =>
    el.classList.toggle("active", i === srActive));
}
function pickSearch(code) {
  focusRegion(code);
  searchInput.value = "";
  closeSearch();
  searchInput.blur();
}
function closeSearch() {
  searchResults.classList.remove("show");
  searchResults.innerHTML = "";
  srItems = []; srActive = -1;
}
searchInput.addEventListener("input", () => runSearch(searchInput.value));
searchInput.addEventListener("focus", () => { if (searchInput.value.trim()) runSearch(searchInput.value); });
searchInput.addEventListener("keydown", (e) => {
  if (!srItems.length && e.key !== "Escape") return;
  if (e.key === "ArrowDown") { e.preventDefault(); srActive = Math.min(srActive + 1, srItems.length - 1); highlightSR(); }
  else if (e.key === "ArrowUp") { e.preventDefault(); srActive = Math.max(srActive - 1, 0); highlightSR(); }
  else if (e.key === "Enter") { e.preventDefault(); if (srItems[srActive]) pickSearch(srItems[srActive].code); }
  else if (e.key === "Escape") { closeSearch(); searchInput.blur(); }
});
document.addEventListener("click", (e) => {
  if (!$("searchWrap").contains(e.target)) closeSearch();
});

// 이름이 겹칠 때 시도해 볼 세로 이동량(px). 0=제자리부터 위아래로 번갈아 넓혀간다.
const LABEL_NUDGE = [0, -7, 7, -14, 14, -21, 21, -28, 28];
function updateLabels() {
  const rect = svg.getBoundingClientRect();
  if (!rect.width) return;
  const pxPerUnit = rect.width / vb.w;
  svg.querySelector(".g-labels").style.fontSize = (11 / pxPerUnit) + "px";
  provLabelsG.style.fontSize = (13 / pxPerUnit) + "px";
  const showMuni = vb.w <= MAPS[COUNTRY].labelAt;
  provLabelsG.style.display = showMuni ? "none" : "";

  // --- 어떤 시·군·구 이름을 보일지: 큰 지역 우선, 겹치면 위아래로 밀어보고 그래도 안 되면 숨김 ---
  // (수치는 labelMeta에 미리 파싱, 정렬도 미리 해둠 — 팬/줌마다 재계산하지 않음)
  const shownSet = new Set();
  const offsets = new Map();        // code -> 세로 이동량(SVG 단위)
  if (showMuni) {
    const sx0 = rect.left - vb.x / vb.w * rect.width, kx = rect.width / vb.w;
    const sy0 = rect.top - vb.y / vb.h * rect.height, ky = rect.height / vb.h;
    const placed = [];
    for (const m of labelMetaByArea) {
      const isSel = m.code === selected;
      // 선택한 지역은 작아도(폭이 좁아도) 이름을 보여준다
      // (data-w가 라벨 자리의 실제 내부 폭이라 여백을 따로 두지 않는다)
      if (!isSel && m.w * pxPerUnit <= m.len * 11) continue;
      const sx = sx0 + m.x * kx, sy = sy0 + m.y * ky;
      // 겹침 판정 상자는 글자가 차지하는 크기에 맞춘다 (11px 글자 = 글자당 폭 11).
      // 여백이 넉넉하면 실제로는 안 겹치는 이름끼리 서로를 지운다.
      const halfW = m.len * 5.5 + 1;
      const l = sx - halfW, r = sx + halfW;
      // 화면 밖 라벨은 겹침 계산에서 제외 (가장 큰 절감)
      if (r < rect.left || l > rect.right) continue;
      // 자리가 없으면 지역 안에서 위아래로 조금씩 밀어 빈 곳을 찾는다.
      // 이동 한계는 지역 높이의 절반 — 이름이 자기 지역을 벗어나지 않게.
      const room = Math.max(0, m.h * ky / 2 - 7);
      for (let i = 0; i < LABEL_NUDGE.length; i++) {
        const dy = LABEL_NUDGE[i];
        if (Math.abs(dy) > room) continue;
        const t = sy + dy - 7, b = sy + dy + 7;
        if (b < rect.top || t > rect.bottom) continue;
        // 선택한 지역 이름은 겹쳐도 항상 표시 (어느 지역인지 헷갈리지 않게)
        let hit = false;
        if (!isSel) {
          for (let j = 0; j < placed.length; j++) {
            const p = placed[j];
            if (!(r < p.l || l > p.r || b < p.t || t > p.b)) { hit = true; break; }
          }
        }
        if (hit) continue;
        shownSet.add(m.code);
        placed.push({ l, r, t, b });
        if (dy) offsets.set(m.code, dy / ky);
        break;
      }
    }
  }
  const marks = [];
  labelMeta.forEach(m => {
    const t = m.t, code = m.code;
    const labelShown = showMuni && shownSet.has(code);
    t.style.display = labelShown ? "" : "none";
    // 겹침을 피해 밀어낸 만큼 실제로 옮긴다 (바뀔 때만 DOM 갱신)
    const off = labelShown ? (offsets.get(code) || 0) : 0;
    if (m.off !== off) { m.off = off; t.setAttribute("y", m.y + off); }
    if (labelShown) {
      t.classList.toggle("on-visited", visited.has(code));
      t.classList.toggle("sel-label", code === selected);
    }
    if (!visited.has(code)) return;
    const x = m.x, y = m.y, ly = m.y + off;
    // 화면(뷰박스) 밖 마커는 만들지 않는다 — 확대 시 대부분이 여기서 걸러짐
    const pad = Math.max(m.w, m.h);
    if (x + pad < vb.x || x - pad > vb.x + vb.w ||
        y + pad < vb.y || y - pad > vb.y + vb.h) return;
    const emblem = EMBLEMS[code];
    if (emblem) {
      // 지자체 심벌을 지역 안에 반투명하게 채움. 지역 크기에 맞추되 너무 작아지지 않게
      const w = m.w, h = m.h;
      // 심벌이 지역 경계를 넘지 않도록 지역 크기 안에 가둔다.
      const fontSvg = 11 / pxPerUnit;
      const availW = w * 0.86;
      // 라벨이 보이면 심벌은 이름 위쪽 공간에만 (지역 위 경계 ~ 라벨 위끝),
      // 안 보이면 지역 중심에 크게.
      const availH = labelShown
        ? Math.max(0, h / 2 - fontSvg * 0.6 - 2 / pxPerUnit)
        : h * 0.82;
      let size = Math.min(w, h) * 0.6;
      size = Math.min(size, availW, availH, 96 / pxPerUnit);
      size = Math.max(size, 12 / pxPerUnit);          // 너무 작지 않게
      size = Math.min(size, availW, availH);          // 단, 지역을 넘지는 않게 최종 캡
      const half = size / 2;
      let cx = x;
      // 이름이 밀려났으면 심벌도 그 위로 따라간다 (겹치지 않게)
      let cy = labelShown ? (ly - fontSvg * 0.5 - 2 / pxPerUnit - half) : y;
      // 심벌 상자를 지역 bbox 안으로 강제 클램프 (경계 밖으로 삐져나오지 않게)
      const bb = regionBBox(code);
      if (bb) {
        if (bb.width > size) cx = Math.min(Math.max(cx, bb.x + half), bb.x + bb.width - half);
        else cx = bb.x + bb.width / 2;
        if (bb.height > size) cy = Math.min(Math.max(cy, bb.y + half), bb.y + bb.height - half);
        else cy = bb.y + bb.height / 2;
      }
      marks.push(
        `<image href="${emblem}" x="${cx - half}" y="${cy - half}" width="${size}" height="${size}"` +
        ` opacity="0.72" preserveAspectRatio="xMidYMid meet"/>`);
    } else {
      // 휘장이 없는 지역은 체크 핀으로 (줌 무관 고정 크기)
      const s = 1 / pxPerUnit;
      const py = labelShown ? ly - 9 / pxPerUnit : y;
      marks.push(
        `<g transform="translate(${x} ${py}) scale(${s})">` +
        `<path d="M0 0C-4.5-6-8-9.2-8-13a8 8 0 1 1 16 0c0 3.8-3.5 7-8 13Z"` +
        ` fill="#2d6a4f" stroke="#ffffff" stroke-width="1.5"/>` +
        `<path d="M-3.2-14.5-0.8-12 3.4-16.2"` +
        ` stroke="#ffffff" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"/>` +
        `</g>`);
    }
  });
  marksG.innerHTML = marks.join("");
}
function zoomAt(cx, cy, factor) {
  const newW = Math.min(home.w * 2, Math.max(home.w / 20, vb.w * factor));
  const k = newW / vb.w;
  vb.x = cx - (cx - vb.x) * k;
  vb.y = cy - (cy - vb.y) * k;
  vb.w = newW; vb.h = vb.h * k;
  applyVB();
}
function clientToSvg(e) {
  const r = svg.getBoundingClientRect();
  return [vb.x + (e.clientX - r.left) / r.width * vb.w,
          vb.y + (e.clientY - r.top) / r.height * vb.h];
}
MAPSVGS.forEach(el => el.addEventListener("wheel", e => {
  e.preventDefault();
  const [cx, cy] = clientToSvg(e);
  zoomAt(cx, cy, e.deltaY > 0 ? 1.2 : 1 / 1.2);
}, { passive: false }));
$("zoomIn").onclick = () => zoomAt(vb.x + vb.w / 2, vb.y + vb.h / 2, 1 / 1.4);
$("zoomOut").onclick = () => zoomAt(vb.x + vb.w / 2, vb.y + vb.h / 2, 1.4);
$("zoomReset").onclick = () => { vb = { ...home }; applyVB(); };
window.addEventListener("resize", updateLabels);

const pointers = new Map();
let pinchDist = 0;
let tapStart = null, tapMoved = false;   // 탭 판정용
MAPSVGS.forEach(el => el.addEventListener("pointerdown", e => {
  pointers.set(e.pointerId, e);
  if (pointers.size === 1) { tapStart = [e.clientX, e.clientY]; tapMoved = false; }
  else { tapStart = null; }   // 멀티터치면 탭 아님
  if (pointers.size === 2) {
    const [a, b] = [...pointers.values()];
    pinchDist = Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY);
  }
  el.setPointerCapture(e.pointerId);
}));
MAPSVGS.forEach(el => el.addEventListener("pointermove", e => {
  if (!pointers.has(e.pointerId)) return;
  const prev = pointers.get(e.pointerId);
  pointers.set(e.pointerId, e);
  const r = svg.getBoundingClientRect();
  if (pointers.size === 1) {
    if (tapStart && Math.hypot(e.clientX - tapStart[0], e.clientY - tapStart[1]) > 8) tapMoved = true;
    const dx = (e.clientX - prev.clientX) / r.width * vb.w;
    const dy = (e.clientY - prev.clientY) / r.height * vb.h;
    if (dx || dy) { el.classList.add("dragging"); vb.x -= dx; vb.y -= dy; applyVB(); }
  } else if (pointers.size === 2) {
    const [a, b] = [...pointers.values()];
    const d = Math.hypot(a.clientX - b.clientX, a.clientY - b.clientY);
    if (pinchDist > 0 && d > 0) {
      const mx = (a.clientX + b.clientX) / 2, my = (a.clientY + b.clientY) / 2;
      const cx = vb.x + (mx - r.left) / r.width * vb.w;
      const cy = vb.y + (my - r.top) / r.height * vb.h;
      zoomAt(cx, cy, pinchDist / d);
      pinchDist = d;
    }
  }
}));
["pointerup","pointercancel"].forEach(ev => MAPSVGS.forEach(el => el.addEventListener(ev, e => {
  // 단일 탭(거의 안 움직임)이면 지역 선택
  if (ev === "pointerup" && tapStart && !tapMoved && pointers.size === 1) {
    handleTap(e.clientX, e.clientY);
  }
  pointers.delete(e.pointerId);
  if (pointers.size === 0) { el.classList.remove("dragging"); tapStart = null; }
  pinchDist = 0;
})));

$("recMonth").addEventListener("change", e => { recMonth = +e.target.value; renderRecs(); });

// ---- 기록 검색 / 기간 필터 ----
let feedSearchTimer = 0;
$("feedSearch").addEventListener("input", e => {
  const v = e.target.value;
  clearTimeout(feedSearchTimer);
  feedSearchTimer = setTimeout(() => { feedQuery = v; renderFeed(); }, 150);
});
$("feedYear").addEventListener("change", e => { feedYear = e.target.value; renderFeed(); });

// ---- 백업 / 복원 ----
// 사진은 파일이 아니라 주소만 담는다. 저장소에 원본이 남아 있으면 복원 시 다시 연결되지만,
// 원본 지도를 삭제하면 사진 파일도 함께 지워지므로 그때는 복원되지 않는다.
$("backupBtn").onclick = () => {
  const data = {
    app: "map-for-memory", version: 1, exportedAt: new Date().toISOString(),
    room: ROOM, name: $("roomName").textContent,
    visited: [...visited], notes, photos,
  };
  const blob = new Blob([JSON.stringify(data, null, 2)], { type: "application/json" });
  const a = document.createElement("a");
  a.href = URL.createObjectURL(blob);
  a.download = `여행지도_${data.name || ROOM}_${new Date().toISOString().slice(0, 10)}.json`;
  a.click();
  setTimeout(() => URL.revokeObjectURL(a.href), 1000);
};
$("restoreBtn").onclick = () => $("restoreFile").click();
$("restoreFile").addEventListener("change", async e => {
  const file = e.target.files[0];
  e.target.value = "";
  if (!file) return;
  let data;
  try { data = JSON.parse(await file.text()); } catch { alert("백업 파일을 읽을 수 없습니다."); return; }
  if (!data || data.app !== "map-for-memory") { alert("이 앱의 백업 파일이 아닙니다."); return; }
  const cnt = (data.visited || []).length;
  if (!confirm(`방문 ${cnt}곳과 기록을 지금 지도에 합칩니다.\n기존 기록은 지워지지 않고, 겹치는 항목은 건너뜁니다.\n\n사진은 원본이 지워지지 않았을 때만 함께 복원됩니다.`)) return;
  $("restoreBtn").disabled = true;
  const before = { v: visited.size, n: Object.keys(notes).length };
  try {
    await restoreBackup(data);
    alert(`복원 완료 — 방문 ${visited.size - before.v}곳, 기록 ${Object.keys(notes).length - before.n}곳이 추가되었습니다.`);
  } catch (err) {
    alert("복원 중 문제가 발생했습니다: " + (err.message || err));
  } finally {
    $("restoreBtn").disabled = false;
  }
});
// 합치기 방식 — 이미 있는 방문·기록·사진은 그대로 두고 없는 것만 채운다
async function restoreBackup(data) {
  for (const raw of data.visited || []) {
    const code = mergedTargetOf(String(raw)) || String(raw);
    if (!nameByCode[code] || visited.has(code)) continue;
    visited.add(code); markDirty(code);
    await api("/api/visited", { method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ room: ROOM, code, on: true }) });
  }
  for (const [raw, note] of Object.entries(data.notes || {})) {
    const code = mergedTargetOf(String(raw)) || String(raw);
    if (!nameByCode[code]) continue;
    const cur = getVisits(code);
    const add = (note.visits || []).filter(v =>
      !cur.some(c => c.id === v.id || (c.start === v.start && c.end === v.end && c.memo === v.memo)));
    if (!add.length) continue;
    const merged = cur.concat(add.map(v => ({ id: v.id || newVid(), start: v.start || "", end: v.end || "", memo: v.memo || "" })));
    notes[code] = { visits: merged }; markDirty(code);
    await api("/api/note", { method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ room: ROOM, code, visits: merged }) });
  }
  for (const [raw, list] of Object.entries(data.photos || {})) {
    const code = mergedTargetOf(String(raw)) || String(raw);
    if (!nameByCode[code]) continue;
    photos[code] = photos[code] || [];
    for (const p of list || []) {
      if (!p || !p.url || photos[code].some(x => x.url === p.url)) continue;
      await api(`/api/photo?room=${ROOM}&code=${code}&link=1`, {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ url: p.url, name: p.name || "", vid: p.vid || "", thumb: p.thumb || "" }) });
      photos[code].push({ url: p.url, name: p.name || "", vid: p.vid || "", thumb: p.thumb || "" });
    }
    if (!photos[code].length) delete photos[code];
  }
  paintVisited(); render(); updateLabels(); renderFeed();
  if (selected) renderPanel();
}

// ---- 지도 삭제 ----
$("deleteMapBtn").onclick = () => {
  $("delDesc").innerHTML =
    `<b>${escapeHtml($("roomName").textContent)}</b> 지도를 완전히 삭제합니다.<br>` +
    `방문 표시·기록·사진이 모두 지워지며 <b>되돌릴 수 없습니다.</b><br>` +
    `함께 쓰는 사람도 더 이상 열 수 없습니다.`;
  $("delPw").value = "";
  $("delErr").textContent = "";
  $("delModal").classList.add("show");
  $("delPw").focus();
};
$("delCancel").onclick = () => $("delModal").classList.remove("show");
$("delModal").onclick = (e) => { if (e.target.id === "delModal") $("delModal").classList.remove("show"); };
$("delPw").onkeydown = (e) => { if (e.key === "Enter") $("delConfirm").click(); };
$("delConfirm").onclick = async () => {
  const pw = $("delPw").value;
  if (!pw) { $("delErr").textContent = "비밀번호를 입력해 주세요."; return; }
  $("delConfirm").disabled = true;
  $("delErr").textContent = "";
  try {
    await api("/api/rooms", { method: "DELETE", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ room: ROOM, password: pw }) });
    delete savedRooms[ROOM];
    localStorage.setItem("travelRooms", JSON.stringify(savedRooms));
    try { localStorage.removeItem("state:" + ROOM); } catch {}
    location.href = "/";
  } catch (e) {
    $("delErr").textContent = e.message === "Failed to fetch" ? "서버 연결 실패" : (e.message || "삭제 실패");
    $("delConfirm").disabled = false;
  }
};

// ---- 내보내기 / 가져오기 / 초기화 ----
// 초기화 — 지금 보고 있는 나라의 방문 표시만, 지도 비밀번호 확인 후 실행
$("resetBtn").onclick = () => {
  const cName = COUNTRY === "jp" ? "일본" : "한국";
  let n = 0;
  visited.forEach(c => { if (countryOfCode(c) === COUNTRY && nameByCode[c]) n++; });
  $("resetDesc").innerHTML =
    `<b>${cName}</b>의 방문 표시 <b>${n}곳</b>이 지워집니다.<br>` +
    `사진·메모는 그대로 남고, 다른 나라 기록은 유지됩니다.<br>함께 쓰는 지도라면 모두에게 적용됩니다.`;
  $("resetPw").value = "";
  $("resetErr").textContent = "";
  $("resetModal").classList.add("show");
  $("resetPw").focus();
};
$("resetCancel").onclick = () => $("resetModal").classList.remove("show");
$("resetModal").onclick = (e) => { if (e.target.id === "resetModal") $("resetModal").classList.remove("show"); };
$("resetPw").onkeydown = (e) => { if (e.key === "Enter") $("resetConfirm").click(); };
$("resetConfirm").onclick = async () => {
  const pw = $("resetPw").value;
  if (!pw) { $("resetErr").textContent = "비밀번호를 입력해 주세요."; return; }
  $("resetConfirm").disabled = true;
  $("resetErr").textContent = "";
  try {
    await api("/api/reset", {
      method: "POST", headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ room: ROOM, password: pw, country: COUNTRY })
    });
    // 현재 나라 방문만 제거
    [...visited].forEach(c => { if (countryOfCode(c) === COUNTRY) visited.delete(c); });
    paintVisited();
    render(); updateLabels(); renderFeed();
    if (selected) renderPanel();
    setBadge(true);
    $("resetModal").classList.remove("show");
  } catch (e) {
    $("resetErr").textContent = e.message === "Failed to fetch" ? "서버 연결 실패" : (e.message || "초기화 실패");
  } finally {
    $("resetConfirm").disabled = false;
  }
};

// ---- 오프라인 지원 (서비스워커) ----
if ("serviceWorker" in navigator && location.protocol !== "file:") {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("/sw.js").catch(() => {});
  });
}
// 오프라인일 때 안내
function updateOnlineBadge() {
  if (!navigator.onLine) setBadge(false, "오프라인 (보기 전용)");
  else setBadge(true);
}
window.addEventListener("offline", updateOnlineBadge);
window.addEventListener("online", () => { updateOnlineBadge(); if (ROOM) refresh(); });

// ---- 시작 ----
const roomParam = new URLSearchParams(location.search).get("room");
if (roomParam && /^[a-z0-9]{6,12}$/.test(roomParam)) enterRoom(roomParam);
else showLanding();
